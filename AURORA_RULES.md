---
title: Aurora 项目规则与开发上下文
status: approved
owner: architecture
last-reviewed: 2026-07-27
applies-to: Aurora 仓库全部 Agent、代码、测试、文档与技术决策
related:
  - AGENTS.md
  - Auroa-PRD-业务逻辑汇总-v2.1-核心业务定稿版.md
  - "Aurora 架构规范.md"
  - "Aurora 代码规范.md"
  - "Aurora 测试规范.md"
  - "Aurora 文档规范.md"
  - "Aurora ADR 规范.md"
supersedes: none
maintenance: append-only
---

# Aurora 项目规则与开发上下文

## 1. 文件定位与使用方式

本文件是 Aurora 规则体系的最高级入口、当前开发上下文快照和 Agent 工作流程规范。

新的 Agent 或新的开发对话只阅读本文件，应当能够理解产品目标、当前阶段、系统边界、关键业务规则、工程质量要求、正式文档位置以及完成任务所需的标准流程。

本文件不取代六份长期规范。Agent 在第一次修改代码或正式文档前，必须继续完整阅读六份规范原文；本文件负责恢复上下文、建立规则索引和提供跨领域执行流程。

规则解释顺序：

1. 当前任务的明确目标和已批准范围；
2. 本文件中的项目级原则和开发流程；
3. 六份长期规范中的领域详细规则；
4. 已经 accepted 的 ADR；
5. 模块 README、公共 API、协议、测试、发布和运维文档；
6. 局部实现说明和代码注释。

发现规则冲突时，不得自行选择最方便的解释。必须暂停受冲突影响的实现，记录冲突文件、规则编号、影响范围和可选处理方式，请求项目负责人决策，再通过追加记录消除冲突。

## 2. 当前项目阶段

截至 2026-07-27，Aurora 处于“核心业务规则冻结、工程实现准备”阶段。

当前已经具备：

- 第一版核心业务 PRD；
- 五大系统和 SDK 分层架构；
- TypeScript 和 SDK 安全代码规范；
- 分层测试和 CI 质量规范；
- 文档长期维护规范；
- ADR 决策治理规范；
- 六项初始架构 ADR 提案；
- 项目级 Agent 开发流程。

当前尚未由正式文档确认：

- 具体前端和服务端框架；
- 数据库、消息队列、缓存和搜索技术；
- Monorepo 工具和包管理器；
- 构建、发布和部署平台；
- 浏览器支持版本矩阵；
- SDK 包体积和性能预算的具体数值；
- 正式模块目录和公共 API 实现。

Agent 不得把尚未确认的技术选择当作既定事实。涉及长期技术影响时必须先创建或更新 ADR，并等待 accepted。

## 3. 产品定位

Aurora 是面向前端开发团队的监控 SaaS，主要服务独立开发者、小型前端团队和中小型研发团队。

第一版核心闭环：

```text
创建项目
→ 安装并初始化 SDK
→ 接收错误、请求异常和性能数据
→ 将相似事件聚合为问题
→ 查看源码、调用栈和安全上下文
→ 分配并处理问题
→ 标记解决
→ 新版本或解决时间后再次发生时重新打开
→ 通过基础告警及时发现异常
```

第一版成功标准是用户能够稳定完成这条闭环，而不是堆积功能数量。

第一版不是：

- 完整工单和审批系统；
- 团队即时通讯工具；
- 完整用户行为分析平台；
- Session Replay 平台；
- 全链路后端可观测平台；
- 通用 BI 或可编程查询平台；
- 大型企业组织治理平台；
- 收费、账单和支付系统；
- AI 根因分析系统。

新增能力只有在直接保障接入、数据正确、问题定位、处理闭环、权限、隐私、删除或告警解释时，才能进入第一版。

## 4. 第一版产品规则摘要

### 4.1 账号、组织和项目

- 支持邮箱注册、验证、登录、找回密码、修改密码、退出和注销。
- 注册后自动创建个人工作空间。
- 组织隔离成员、项目、资源用量、私密令牌和审计记录。
- 组织角色为所有者、管理员和成员。
- 项目角色为项目管理员、开发成员和只读成员。
- 第一版不创建责任小组、自定义角色或复杂审批。
- 创建项目必须原子完成项目、默认生产环境、客户端上报密钥、接入进度和创建审计。
- 网络重试使用唯一操作编号保证幂等，不能重复创建项目。

### 4.2 SDK 接入判定

接入引导为安装 SDK、初始化 SDK、发送测试错误三步。

仅收到请求、性能数据或普通合法事件，不代表完整接入成功。只有测试错误完成以下链路后，状态才是“接入成功”：

```text
SDK 发送测试错误
→ 同步接入校验通过
→ 进入可靠缓冲
→ 异步保存事件
→ 异步聚合为测试问题
→ 接入状态变为 connected
```

上报接口返回“已接收”只表示数据可靠进入缓冲，不表示存储、问题聚合、Source Map、指标或告警已经完成。

测试问题不进入默认健康状态、不触发告警，并允许清理明确标记的测试数据。

### 4.3 默认采集

默认 100% 采集：

- JavaScript 未捕获异常；
- 未处理 Promise 异常；
- React 和 Vue 框架错误；
- 开发者主动上报错误；
- 关键静态资源加载失败；
- 请求网络失败和超时；
- HTTP 429；
- HTTP 500—599。

普通 4xx 默认不创建请求问题。慢请求默认阈值为 3,000 毫秒、采样率为 20%。页面性能默认采集 LCP、INP、CLS 和页面加载耗时，采样率为 10%。

请求默认只监控同源 fetch 和 XMLHttpRequest。跨域业务接口必须通过允许列表显式加入；不得使用星号监控全部互联网来源。SDK 自身上报请求必须排除，避免递归监控。

URL 默认移除查询参数值。只自动替换数字、UUID、长随机串和常见哈希等高置信度动态路径段。开发者路径模板优先；无法确定时不得猜测。

错误默认附带最多 30 条安全操作轨迹。轨迹只用于错误定位，不用于用户行为分析。

### 4.4 永远默认关闭的数据

安装 SDK 不得自动采集：

- 请求体和响应体；
- Cookie；
- Authorization 等认证请求头；
- 表单输入、密码和验证码；
- 页面完整 DOM 或页面文本；
- 完整点击和鼠标轨迹；
- 会话回放；
- 剪贴板；
- 控制台日志正文；
- 完整 IP 作为长期用户身份。

扩大采集必须由开发者显式配置，且仍不能绕过客户端和服务端隐私过滤。

### 4.5 事件处理顺序

客户端处理顺序：

```text
捕获
→ 规范化
→ 默认敏感信息过滤
→ beforeSend
→ 采样
→ 有上限的内存队列
→ 批量发送和有限重试
```

服务端同步接入阶段：

```text
鉴权
→ 来源、环境、大小和协议校验
→ 运行时 Schema 校验
→ 服务端隐私过滤
→ 去重、限流和接入采样
→ 可靠队列或持久化缓冲
→ 快速返回接收结果
```

服务端异步处理阶段：

```text
幂等消费
→ 数据清洗和标准化
→ 事件或最小统计存储
→ Source Map 解析
→ 问题分组
→ 性能聚合
→ 告警计算
```

接收成功的数据不能因进程重启直接丢失；处理失败必须有限重试；无法处理的数据必须进入失败记录或死信；队列积压必须可监控。

### 4.6 可靠性

- 客户端为事件生成唯一编号，同一事件重试编号不变。
- 服务端按事件编号去重。
- 单条事件错误不得导致整批失败。
- 网络失败、超时、临时不可用和带等待时间的限流可以有限重试。
- 无效密钥、来源禁止、环境禁止、格式错误、体积超限、隐私拒绝和项目归档不得重试。
- 第一版只使用页面生命周期内的内存队列，不做浏览器持久化离线队列。
- 队列容量、批次大小、重试次数和退避时间必须有上限。
- 页面退出前只尽力发送高价值错误，不执行长时间异步任务。

### 4.7 用户与影响用户统计

开发者可主动设置业务用户编号。业务用户编号优先用于去重；未设置时使用项目范围内的匿名浏览器编号。

匿名编号：

- 不跨项目或组织；
- 不使用完整 IP；
- 不使用设备或浏览器指纹；
- 不跨浏览器、设备或清理后的存储合并；
- 不自动与后续登录身份合并；
- 可以由项目管理员关闭。

影响用户数必须标注为估算值，并说明采样、设备变化和匿名标识限制。

### 4.8 发布与 Source Map

- 发布版本用于筛选、部署、Source Map 和再次出现判断。
- Source Map 严格按照项目、发布版本和构建文件路径匹配。
- 允许移除协议、域名和已配置 CDN 前缀，但保留实际文件名和哈希。
- 禁止跨版本尝试、相近版本猜测和模糊文件名匹配。
- 解析失败保留原始堆栈并展示明确原因。
- 同一版本和路径上传不同文件时不得静默覆盖，必须确认并记录审计。
- 重新解析只处理完整事件保留期内、未成功解析或受替换影响的事件。
- Source Map 成功后可以改善后续聚合，但不自动重组全部历史问题。

### 4.9 事件与问题

Event 表示一次真实发生；Issue 表示相似事件聚合结果。

错误问题指纹优先使用：

```text
错误类型
+ 源码映射后的关键调用栈位置
+ 高置信度归一化错误信息
```

没有有效堆栈时，使用错误类型、归一化信息和页面或接口。自定义 fingerprint 优先，但不能包含敏感原始值，也不能绕过服务端校验。

问题总量和完整样本必须分开：

- 总次数、趋势、影响用户、环境、版本、浏览器和页面分布持续更新；
- 每个问题建议最多保留最近 100 条完整代表样本；
- 首次、最新、再次出现、新版本、新环境、新浏览器、新页面和高严重级别样本优先；
- 高频普通重复事件可以只更新计数；
- 采样或额度降级时必须显示数据完整性提示。

第一版支持问题合并，不提供复杂历史条件拆分。

### 4.10 问题生命周期

人工状态只有 open、in_progress、resolved 和 ignored。

- 每个问题最多一名负责人。
- 单个未分配问题开始处理时，在同一事务内自动分配给当前操作人。
- 批量改为处理中时不自动分配。
- 标记解决可指定解决版本或使用解决时间。
- 解决版本及以后再次发生，或解决时间后真实发生时，问题重新打开。
- 永久忽略或定时忽略的问题仍进入整体指标，但不进入默认待处理列表。
- 系统活动不可编辑或删除。
- 成员备注保持简单，不支持附件、多层回复、完整版本历史和提及通知。
- 当前页批量操作建议最多 100 条，不提供全结果后台批处理。

### 4.11 告警

第一版只提供固定阈值和站内通知。

支持错误数、新问题数、再次出现数、请求失败率、慢请求数以及 LCP、INP、CLS 超标比例。

告警必须区分：

- 统计窗口；
- 触发阈值；
- 触发持续时间；
- 独立恢复阈值；
- 恢复持续时间；
- 冷却时间；
- 比例指标最小样本数。

无数据、项目归档、接入异常、额度降级或处理延迟不能直接判断为恢复，应显示数据不足或计算暂停。

### 4.12 权限、审计、额度和删除

后端必须对每次操作校验权限，不能只依赖前端隐藏按钮。客户端上报密钥只能写入监控数据；私密令牌用于 CLI、CI/CD、发布、部署和 Source Map 管理，不能进入浏览器。

当前额度只用于资源保护，不涉及收费。接近额度时按固定顺序降低页面性能、慢请求、低价值资源错误和高频重复样本；优先保留新问题、再次出现、严重错误、新版本和接入测试事件。降级后必须提示数据不完整。

默认保留期：

- 完整事件详情 30 天；
- 分钟聚合 30 天；
- 小时聚合 90 天；
- 天聚合 1 年；
- 问题、发布和部署记录在项目存在期间保留；
- 安全审计摘要 1 年。

项目归档后停止接收新事件和告警，但可恢复。删除进入默认 7 天回收站，期满永久清理事件、聚合、问题、备注、发布、Source Map、成员关系和密钥，只保留无监控内容的最小安全审计摘要。

## 5. 架构边界

### 5.1 五大系统

1. SDK：采集、规范化、过滤、脱敏、采样、缓存和批量上报。
2. 数据接入：鉴权、限流、协议校验、非法数据过滤、可靠接收和快速响应。
3. 数据处理与存储：清洗、去重分组、Source Map、聚合、存储、索引、保留和告警计算。
4. 管理平台：项目、问题、性能、请求、告警、Source Map、成员和权限管理。
5. 公共协议：事件结构、运行时 Schema、枚举、版本和兼容策略。

管理平台只能通过公开 API 访问数据，禁止直连数据库或内部队列。数据处理不得依赖 SDK 或管理平台内部实现。

### 5.2 SDK 分层

允许依赖方向：

```text
event-schema
↑
core
↑
browser
↑
采集插件和框架适配
```

Core 必须环境无关，不能直接访问 window、document、navigator、location、XMLHttpRequest、fetch 或浏览器存储。环境能力通过接口注入。

插件只负责监听、捕获、转换和释放资源，禁止直接发送网络请求、维护独立队列、绕过统一采样和脱敏、访问 Core 私有状态或依赖其他插件内部实现。

框架适配只连接框架生命周期和上下文，不复制 Core、Browser 或插件已有能力。

### 5.3 依赖和模块

- 所有代码统一在 Monorepo 管理。
- apps 放可部署应用，packages 放可复用包，examples 放示例，docs 放跨模块文档，tooling 放工程工具。
- 跨包只能使用公开导出入口。
- 禁止相对路径访问其他包的 src、internal 或私有文件。
- 禁止循环依赖、反向依赖和为解决循环依赖把业务代码扔进 shared。
- shared 只容纳稳定、无业务倾向、被多个系统复用的基础能力。
- 架构约束必须通过 TypeScript、Lint、包导出、依赖检查、契约测试或 CI 执行。

## 6. 代码规则

- 全项目默认严格 TypeScript。
- 外部输入先定义为 unknown，经过运行时 Schema 校验后才能进入业务逻辑。
- 公共函数声明明确参数和返回类型。
- 禁止无说明 any、@ts-ignore、宽泛对象类型和用断言代替校验。
- 文件、类型、变量和布尔命名遵守统一规则，名称表达业务含义。
- 函数约 50 行、文件约 300 行、参数 4 个、嵌套 3 层是评审预警线，不是机械拆分目标。
- 函数、文件和模块保持单一职责。
- 错误不得静默吞掉。
- 日志不得包含密码、Token、Cookie、Authorization、完整请求响应或未脱敏用户输入。
- 公共 API 保持最小、稳定，说明默认值、重复调用、生命周期、异步和失败行为。
- 不兼容公共 API 或协议变更必须有 accepted ADR、迁移方案和版本策略。
- SDK 内部异常不得冒泡到宿主应用。
- 单个插件异常不得阻断其他插件。
- 原生 API 代理必须保存原实现、防止重复代理、保持语义并可完整恢复。
- destroy 后必须释放监听器、定时器和代理。
- SDK 不得深度遍历完整 DOM、序列化全局状态、同步解析 Source Map、无限递归或无限缓存。
- 多个 Aurora 实例必须隔离，不能由不可控全局单例互相污染。

## 7. 测试规则

采用大量单元测试、适量集成测试和少量关键端到端测试。

必须根据风险覆盖：

- Core 初始化、重复初始化、失败、销毁和多实例隔离；
- 插件注册、重复注册、启动、停止、异常隔离和代理恢复；
- 事件管道、过滤、脱敏、采样、队列、批量发送、失败、退避和重试上限；
- SDK 不影响宿主页面；
- 请求插件不递归采集自身上报；
- event-schema 合法、非法、边界和版本兼容样本；
- 数据接入、可靠缓冲和异步处理协作；
- 关键浏览器能力以及 React、Vue 接入；
- 核心端到端监控闭环。

Bug 修复原则上必须增加回归测试，并证明修复前能够复现、修复后通过。

覆盖率初始基线：

- Core、event-schema、脱敏、采样、队列、重试、协议转换和接入校验：行覆盖率至少 85%，分支至少 80%；
- 其他业务模块：行覆盖率至少 70%，分支至少 60%。

PR 阶段必须通过类型、静态检查、受影响单元和集成测试、契约测试、SDK 构建、包入口、核心真实浏览器测试和覆盖率门槛。完整浏览器矩阵、全量 E2E、稳定性、内存泄漏和压力测试在正式发布前执行。

不稳定测试不得通过反复重跑、长期跳过或删除测试来掩盖。

## 8. 文档和 ADR 治理

### 8.1 六份长期规范

以下文件不得删除、移动、重命名或复制成替代版本：

- [核心业务 PRD](Auroa-PRD-业务逻辑汇总-v2.1-核心业务定稿版.md)
- [架构规范](<Aurora 架构规范.md>)
- [代码规范](<Aurora 代码规范.md>)
- [测试规范](<Aurora 测试规范.md>)
- [文档规范](<Aurora 文档规范.md>)
- [ADR 规范](<Aurora ADR 规范.md>)

PRD 历史文件名中的 Auroa 只为引用兼容，正式产品名为 Aurora。

### 8.2 文档同步

同一变更必须同步更新受影响的 PRD、架构、模块 README、SDK、API、协议、测试、发布、迁移、运维或隐私文档。不得用无负责人和期限的“后续补文档”替代必要同步。

正式文档必须说明范围、默认行为、失败行为、不支持范围和兼容性。示例必须能通过类型检查、契约测试、示例构建或 CI 验证。

### 8.3 ADR 门禁

出现以下变化必须先创建 ADR：

- 一级系统、模块职责或依赖方向变化；
- SDK 核心分层变化；
- 核心数据库、队列、缓存、搜索、存储或部署架构变化；
- 同步接收和异步处理模型变化；
- 公共协议兼容策略变化；
- 公共 API 不兼容变化；
- 默认采集、安全、隐私、脱敏或浏览器承诺变化；
- 全项目核心框架或长期迁移成本较高的技术选择。

ADR 状态为 proposed 时不得约束正式实现。只有经过非作者及必要领域评审并标记 accepted 后，才能作为正式决策。accepted 不代表 implemented；实施状态必须单独记录。

已批准 ADR 的结论不得静默改写。变化时创建新 ADR、声明 supersedes、保留旧 ADR，并同步架构、代码、测试和迁移文档。

## 9. Agent 开发流程

### 9.1 阶段一：恢复上下文

新对话开始时：

- [ ] 完整阅读本文件；
- [ ] 完整阅读六份长期规范；
- [ ] 确认当前任务对应 PRD 范围；
- [ ] 读取相关 accepted ADR；
- [ ] 读取涉及模块 README、公共 API、协议和测试说明；
- [ ] 检查工作区状态、现有文件和用户未提交修改；
- [ ] 确认不存在未解决的规则冲突。

未完成以上步骤，不得修改代码或正式文档。

### 9.2 阶段二：分类和范围判断

判断任务属于：

- 回答或评审：只读检查并提供证据，不执行未授权修改；
- 诊断：确定原因和影响，不自动实施修复；
- Bug 修复：先复现，再编写回归测试，实施最小修复；
- 功能开发：确认 PRD 范围、接口和验收标准；
- 重构：证明不改变公开行为，保持架构边界；
- 协议或公共 API：执行兼容性分析和 ADR 判断；
- 文档：保持权威来源、链接和追加式历史；
- 发布：完成全量验证、版本和迁移说明。

Agent 不得把诊断请求扩展成修复，也不得把局部修改扩展成新平台能力。

### 9.3 阶段三：ADR 判断

开始设计前逐项确认：

- 是否改变系统或模块职责；
- 是否改变依赖方向；
- 是否新增长期基础设施；
- 是否修改 SDK 分层；
- 是否修改公共协议兼容；
- 是否产生公共 API 不兼容变化；
- 是否改变默认采集、隐私或浏览器承诺；
- 是否具有高迁移或回滚成本。

任一项为是时，先创建 proposed ADR。未经 accepted，不得实施受决策影响的正式代码。

### 9.4 阶段四：设计和计划

设计必须明确：

- 目标和非目标；
- 输入、输出和公开接口；
- 所属系统和模块；
- 允许与禁止依赖；
- 正常、失败和边界行为；
- 初始化、重复调用、停止和销毁；
- 数据校验、隐私和安全；
- 测试层级和验收证据；
- 文档影响、迁移和回滚。

复杂任务在修改前编写可执行计划。计划应把工作拆成可独立验证的步骤，列出具体文件、命令和预期结果。

### 9.5 阶段五：实现

- 保持修改范围最小，不进行无关重构。
- 不覆盖用户已有修改。
- 新功能和 Bug 修复优先采用测试驱动方式。
- 外部输入先校验。
- 使用 event-schema 生成和读取事件。
- 通过公开入口跨包调用。
- SDK 监听、代理、队列和定时器必须有明确生命周期和上限。
- 对异常路径、并发、幂等、重复初始化、重试和资源释放进行显式处理。
- 代码注释解释原因、限制和取舍，不重复代码表面行为。

### 9.6 阶段六：验证

完成声明前必须运行与风险匹配的真实命令并读取完整输出。

最低验证顺序：

1. 直接针对修改的测试；
2. 受影响模块的单元和集成测试；
3. TypeScript 和静态检查；
4. event-schema 契约测试；
5. SDK 构建和包导出检查；
6. 必要的真实浏览器测试；
7. 关键链路端到端测试；
8. 文档链接、示例和元数据检查；
9. 版本控制差异和无意修改检查。

不得以“看起来正确”“应该通过”或只运行部分无关检查代替证据。

### 9.7 阶段七：文档同步

每次变更填写文档影响判断：

- [ ] PRD；
- [ ] 架构或 ADR；
- [ ] 模块 README；
- [ ] SDK 或公共 API；
- [ ] event-schema 和协议；
- [ ] 测试和兼容矩阵；
- [ ] 隐私和安全；
- [ ] 发布、迁移或运维；
- [ ] 无文档影响，并说明原因。

规则变化必须追加到对应领域规范，随后同步本文件和 AGENTS.md 的当前有效约束。禁止只修改汇总文件。

### 9.8 阶段八：交付

交付说明必须包含：

- 实际修改结果；
- 关键设计和取舍；
- 修改或新增文件；
- 执行的验证命令及结果；
- 未解决风险或未完成事项；
- 迁移、回滚或发布注意事项；
- 关联 PRD、ADR、Issue、提交或 PR。

如果验证失败、规则冲突、缺少批准或需要扩大授权，必须报告实际状态，不得声称完成。

## 10. 专项任务规则

### 10.1 Bug 修复

```text
复现问题
→ 定位根因
→ 编写失败的回归测试
→ 实施最小修复
→ 回归测试通过
→ 运行受影响测试
→ 更新必要文档
```

无法自动化时，必须记录原因、替代验证、剩余风险和后续测试任务。

### 10.2 公共协议和 API

- 先判断兼容性。
- 删除字段、改变类型或含义、可选改必填、枚举语义变化均不兼容。
- 不兼容变更必须有 accepted ADR、迁移方案和主版本策略。
- SDK、接入和处理共享同一组契约样本。
- 管理平台只依赖公开查询 API，不依赖存储内部结构。

### 10.3 SDK 插件

- 插件只采集并生成标准事件。
- 所有事件进入统一事件管道。
- 重复启用不得重复监听。
- stop 或 destroy 后不再采集并恢复代理。
- 插件失败不得影响宿主或其他插件。
- 上报请求不得被请求插件递归采集。
- 采集字段必须有长度、数量和深度上限。

### 10.4 隐私和安全

任何新增字段、默认采集变化、用户身份变化、日志变化或敏感数据权限变化都必须：

- 更新 PRD、SDK、协议和安全文档；
- 评估是否需要 ADR；
- 增加客户端和服务端过滤测试；
- 证明客户端配置不能绕过服务端禁止规则；
- 证明无权限用户和客户端密钥不能读取数据。

### 10.5 文档规则修改

- 不删除、移动或重命名六份长期规范。
- 不直接改写历史原则。
- 非语义校正追加维护记录。
- 新规则使用 proposed、approved、deprecated 或 superseded 状态。
- 只有 approved 规则正式生效。
- 替代规则必须声明 supersedes 并保留旧规则。
- 每次追加后检查链接、标题、围栏、元数据和当前规则索引。

## 11. 完成定义

任务只有同时满足以下条件才可以声明完成：

- [ ] 用户目标和批准范围全部实现；
- [ ] 未加入 PRD 明确后置能力；
- [ ] 架构边界和依赖方向保持；
- [ ] 需要的 ADR 已 accepted，或任务不需要 ADR；
- [ ] 外部输入、错误、安全和隐私已处理；
- [ ] 测试覆盖正常、异常、边界和生命周期；
- [ ] 所需验证命令已经运行并通过；
- [ ] Bug 修复包含回归证据；
- [ ] 受影响文档在同一变更中同步；
- [ ] 没有删除或覆盖历史规则；
- [ ] 差异中没有无关或用户已有修改；
- [ ] 交付说明包含验证证据和剩余风险。

## 12. 当前必读文档

| 顺序 | 文档 | 状态 | 用途 |
|---:|---|---|---|
| 1 | [AURORA_RULES.md](AURORA_RULES.md) | approved | 开发上下文与规则总入口 |
| 2 | [核心业务 PRD](Auroa-PRD-业务逻辑汇总-v2.1-核心业务定稿版.md) | approved | 产品范围、业务规则和验收 |
| 3 | [架构规范](<Aurora 架构规范.md>) | approved | 系统边界、分层和依赖 |
| 4 | [代码规范](<Aurora 代码规范.md>) | approved | TypeScript、API、错误和 SDK 安全 |
| 5 | [测试规范](<Aurora 测试规范.md>) | approved | 测试分层、覆盖率和 CI |
| 6 | [文档规范](<Aurora 文档规范.md>) | approved | 文档、链接和追加式维护 |
| 7 | [ADR 规范](<Aurora ADR 规范.md>) | approved | 重大决策门禁和演进 |
| 8 | [项目级 Agent 约束](AGENTS.md) | approved | Agent 自动执行入口 |

设计与实施记录：

- [文档治理设计](docs/superpowers/specs/2026-07-27-document-governance-and-agent-workflow-design.md)
- [文档治理实施计划](docs/superpowers/plans/2026-07-27-document-governance-and-agent-rules.md)

## 13. 当前 ADR 状态

以下 ADR 均为 proposed、not-started，只能用于评审，不得作为正式实施批准：

- [ADR-001：统一 Monorepo](docs/adr/ADR-001-use-monorepo.md)
- [ADR-002：五大系统边界](docs/adr/ADR-002-five-system-boundaries.md)
- [ADR-003：SDK 分层插件架构](docs/adr/ADR-003-sdk-plugin-architecture.md)
- [ADR-004：可靠接收与异步处理](docs/adr/ADR-004-asynchronous-event-processing.md)
- [ADR-005：event-schema 单一来源](docs/adr/ADR-005-event-schema-source-of-truth.md)
- [ADR-006：单向依赖与自动约束](docs/adr/ADR-006-one-way-dependencies.md)

## 14. 追加式维护协议

历史规则正文不得删除、覆盖或静默改写。

允许直接修正错别字、编号、链接、Markdown 格式和明确错误的关联编号，但必须在对应文件末尾追加维护记录。

新规则格式：

```markdown
### RULE-YYYYMMDD-NNN：规则标题

- 状态：proposed | approved | deprecated | superseded
- 提出日期：YYYY-MM-DD
- 生效日期：YYYY-MM-DD 或 none
- Owner：模块或角色
- 影响范围：
- 关联规则：
- supersedes：none 或旧规则编号
- 变更原因：
- 新增规则：
- 兼容与迁移说明：
- 验证方式：
```

只有 approved 追加项属于正式规则。proposed 不得约束正式开发。替代旧规则时必须追加 supersedes，旧规则原文继续保留。

复查日期通过末尾追加复查记录维护，不覆盖历史复查信息。

## 15. 规则维护与追加记录

### RULE-BASELINE-20260727：项目规则和 Agent 开发流程基线

- 状态：approved
- 生效日期：2026-07-27
- Owner：architecture
- 影响范围：Aurora 全部 Agent、代码、测试、文档与技术决策
- supersedes：none
- 维护方式：append-only
- 基线说明：本文件首次建立 Aurora 自包含开发上下文、跨领域规则摘要、Agent 端到端开发流程和当前正式文档索引。
- 权威边界：本文件是最高级入口，但六份长期规范继续作为各自领域的详细权威来源。
- 历史保护：后续不得删除、覆盖或静默改写本基线。新增、废弃或替代规则必须追加到本节之后。
- 验证方式：按照本文件第 11 节完成定义以及文档规范的链接、状态和追加记录要求执行。

### RULE-CORRECTION-20260727-001：复查元数据字段规范化

- 状态：approved
- 生效日期：2026-07-27
- Owner：architecture
- 影响范围：本文件元数据
- supersedes：none
- 变更原因：初始整理使用了文档规范未定义的 `initial-reviewed` 字段。
- 校正内容：字段名规范化为 `last-reviewed`，日期保持 2026-07-27。
- 语义影响：none；不改变任何项目规则或 Agent 开发流程。
- 验证方式：文档元数据检查必须识别 `last-reviewed`。

### RULE-20260727-002：ADR 决策状态与实施状态正交

- 状态：approved
- 生效日期：2026-07-27
- Owner：architecture
- 影响范围：本文件第 8.3、9.3、13 节及全部 ADR
- 关联规则：`Aurora ADR 规范.md` 的 ADR-RULE-20260727-001
- supersedes：none
- 新增规则：`not-started` 只表示正式实施尚未开始，不要求 ADR 已经 accepted；因此 `proposed / not-started` 是合法组合。
- 实施门禁：只有 accepted ADR 才能授权正式实施。proposed ADR 即使标记 not-started，也只能用于讨论、调研和明确标记的原型。
- 兼容与迁移说明：六份初始 ADR 状态保持不变。
- 验证方式：分别检查决策状态和实施状态，并确认 proposed ADR 没有被当作实施批准。
