---
title: Aurora 测试规范
status: approved
owner: quality
initial-reviewed: 2026-07-27
applies-to: Aurora SDK、公共协议、服务端、管理平台及示例项目
related:
  - AURORA_RULES.md
  - Auroa-PRD-业务逻辑汇总-v2.1-核心业务定稿版.md
  - "Aurora 架构规范.md"
  - "Aurora 代码规范.md"
  - "Aurora 文档规范.md"
  - "Aurora ADR 规范.md"
supersedes: none
maintenance: append-only
---

# 4. 测试规范

> 规则类别：`TEST`  
> 适用范围：Aurora SDK、公共协议、服务端、管理平台及示例项目  
> 状态：已批准  
> 目标：通过分层测试保障 SDK 稳定性、协议兼容性和核心业务链路，同时控制测试维护成本

## 关联文档

- [项目规则总入口](AURORA_RULES.md)
- [核心业务 PRD](Auroa-PRD-业务逻辑汇总-v2.1-核心业务定稿版.md)
- [架构规范](<Aurora 架构规范.md>)
- [代码规范](<Aurora 代码规范.md>)
- [文档规范](<Aurora 文档规范.md>)
- [ADR 规范](<Aurora ADR 规范.md>)

## 4.1 基本原则

Aurora 测试必须遵守以下原则：

- 采用单元测试、集成测试和端到端测试相结合的分层策略；
- 测试重点由修改风险决定，不机械追求测试数量；
- SDK 必须重点测试生命周期、异常隔离和宿主环境安全；
- 事件协议必须通过共享契约测试保证一致；
- Bug 修复原则上必须增加回归测试；
- 模拟环境用于日常快速测试，真实浏览器用于验证关键能力；
- 核心测试失败必须阻止代码合并；
- 完整兼容性和稳定性测试必须在正式发布前通过；
- 不稳定测试不得被长期忽略。

## 4.2 TEST-001：分层测试

Aurora 采用以下测试结构。

### 单元测试

用于验证独立函数、类和模块。

重点覆盖：

- 配置解析；
- 事件转换；
- 数据过滤；
- 数据脱敏；
- 采样算法；
- 队列管理；
- 重试策略；
- 协议校验；
- 错误分组逻辑；
- 权限和查询条件。

单元测试应当快速、稳定，并尽量避免依赖真实网络和外部服务。

### 集成测试

用于验证多个模块协作后的行为。

重点覆盖：

- Core 与 Browser 层协作；
- Core 与采集插件协作；
- 插件事件进入统一事件管道；
- 队列与上报模块协作；
- 数据接入与公共协议协作；
- 数据接入、队列和数据处理协作；
- 管理平台请求层与页面状态协作。

### 端到端测试

只覆盖关键用户链路，不要求所有功能都编写端到端测试。

至少应覆盖：

    业务页面产生监控事件
    → Aurora SDK 捕获事件
    → SDK 处理并上报
    → 服务端接收和处理
    → 数据可以被查询
    → 管理平台正确展示

端到端测试数量应保持有限，重点保障最核心的业务流程。

## 4.3 TEST-002：SDK Core 测试

SDK Core 必须测试：

- 首次初始化；
- 重复初始化；
- 初始化失败；
- 正常销毁；
- 重复销毁；
- 默认配置；
- 配置合并与更新；
- 插件注册；
- 插件重复注册；
- 插件启动和停止；
- 插件异常隔离；
- 事件管道执行顺序；
- 事件过滤；
- 数据脱敏；
- 采样边界；
- 队列容量限制；
- 批量上报；
- 上报成功；
- 上报失败；
- 重试和退避；
- 达到重试上限后的处理；
- 多个 SDK 实例之间的隔离。

SDK Core 测试必须证明：

- SDK 内部异常不会影响宿主页面；
- 单个插件失败不会阻断其他插件；
- 队列不会无限增长；
- 重试不会无限执行；
- 销毁后资源能够被释放。

## 4.4 TEST-003：采集插件测试

每个采集插件必须验证：

- 能够捕获目标事件；
- 不会误捕获无关事件；
- 产生的事件符合 `event-schema`；
- 数据经过统一事件管道；
- 重复启用不会重复注册监听；
- 停止或销毁后不再采集；
- 原生 API 代理能够恢复；
- 插件异常不会影响宿主页面；
- 敏感字段不会被错误采集；
- 字符串、数组和对象深度限制有效；
- SDK 自身请求不会被递归采集。

例如请求监控插件还应验证：

- `fetch` 请求；
- `XMLHttpRequest` 请求；
- 成功响应；
- 失败响应；
- 超时；
- 请求取消；
- 忽略上报接口；
- 恢复原始请求方法。

错误监控插件还应验证：

- JavaScript 运行时错误；
- Promise 未处理异常；
- 资源加载错误；
- 重复错误；
- 非标准错误对象；
- 空堆栈和异常堆栈格式。

## 4.5 TEST-004：真实浏览器测试

Aurora 日常单元测试和大部分集成测试可以使用模拟浏览器环境。

以下关键能力必须通过真实浏览器自动化测试：

- JavaScript 错误捕获；
- Promise 异常捕获；
- 资源加载错误捕获；
- `fetch` 监控；
- `XMLHttpRequest` 监控；
- History 路由变化；
- 页面性能指标；
- 页面隐藏和恢复；
- 页面刷新和卸载；
- 原生 API 代理与恢复；
- SDK 初始化和销毁；
- SDK 不影响宿主页面；
- Vue 和 React 的核心接入流程。

普通 PR 至少运行核心真实浏览器测试。

正式发布前必须运行完整支持矩阵，包括项目声明支持的：

- Chromium；
- Firefox；
- WebKit 或 Safari 对应环境。

浏览器兼容范围由 Aurora SDK 专项规范定义，测试规范只负责验证已声明的兼容范围。

## 4.6 TEST-005：公共协议契约测试

`event-schema` 是 Aurora 事件协议测试的唯一中心。

必须保证：

- SDK 生成的事件能够通过公共 Schema 校验；
- 数据接入层使用相同 Schema 验证输入；
- 数据处理层能够正确读取合法事件；
- 每种事件类型都有合法样本；
- 每种事件类型都有非法样本；
- 协议版本能够被正确识别；
- 旧协议版本拥有兼容测试；
- 不支持的协议版本会被明确拒绝；
- 字段长度、数组数量和对象深度限制有效。

契约测试至少覆盖：

- 必填字段缺失；
- 可选字段缺失；
- 字段类型错误；
- 非法枚举值；
- 超长字符串；
- 超大数组；
- 非法时间戳；
- 未知事件类型；
- 不支持的协议版本；
- 新旧版本字段兼容。

SDK、数据接入层和数据处理层必须共享一组协议测试样本，禁止各自维护含义不同的数据样本。

管理平台必须基于公开查询接口测试，不得直接依赖原始上报事件的内部存储结构。

## 4.7 TEST-006：Bug 回归测试

Bug 修复原则上必须增加自动化回归测试。

回归测试必须能够：

1.  在修复前复现或证明问题；
2.  在修复后证明问题已解决；
3.  防止后续修改重新引入同类问题。

无法增加自动化测试时，必须在 PR 中说明：

- 为什么无法自动化；
- 使用了什么替代验证方式；
- 仍然存在什么风险；
- 后续是否需要补充测试能力。

以下理由不能单独作为不写回归测试的依据：

- 修改很小；
- 当前时间紧；
- 已经手工测试；
- 代码看起来没有问题。

## 4.8 TEST-007：覆盖率要求

Aurora 采用“风险优先，覆盖率作为最低保障”的策略。

初始覆盖率基线：

    关键核心包：
    行覆盖率不低于 85%
    分支覆盖率不低于 80%

    其他业务模块：
    行覆盖率不低于 70%
    分支覆盖率不低于 60%

关键核心包包括：

- SDK Core；
- `event-schema`；
- 数据脱敏；
- 采样；
- 队列；
- 重试；
- 协议转换；
- 数据接入校验。

覆盖率规则必须遵守：

- 新代码不得明显降低受影响模块的覆盖率；
- 优先测试分支、异常和边界；
- 不得为了覆盖数字编写没有断言价值的测试；
- 覆盖率不是评估测试质量的唯一标准；
- 普通 UI 展示代码和简单胶水代码可以根据风险适当降低要求。

后续调整覆盖率基线时，必须通过追加规则记录完成。

## 4.9 TEST-008：CI 阻断规则

每个 Pull Request 必须通过：

- TypeScript 类型检查；
- 代码静态检查；
- 受影响单元测试；
- 受影响集成测试；
- `event-schema` 契约测试；
- SDK 构建检查；
- SDK 包入口与导出检查；
- 核心真实浏览器测试；
- 覆盖率门槛；
- 与本次修改相关的回归测试。

以上任一检查失败时，不得合并代码。

以下测试可以在主分支、定时任务或正式发布前运行：

- 完整浏览器兼容矩阵；
- 全量端到端测试；
- 全部示例项目接入测试；
- 长时间稳定性测试；
- 内存泄漏测试；
- 大流量压力测试；
- 队列积压测试；
- 完整性能基准测试。

正式发布前，与发布范围相关的完整测试必须通过。

## 4.10 不稳定测试处理

测试出现随机失败时，不得通过反复重跑来掩盖问题。

必须：

- 记录失败现象；
- 判断是产品缺陷还是测试缺陷；
- 创建修复任务；
- 明确负责人；
- 明确修复期限。

严重影响 CI 的不稳定测试可以暂时隔离，但必须：

- 保留测试代码；
- 标记隔离原因；
- 建立修复任务；
- 不得长期跳过关键风险验证。

禁止直接删除失败测试以恢复 CI。

## 4.11 测试数据要求

测试数据必须：

- 不包含真实用户数据；
- 不包含真实 Token、Cookie 或密码；
- 不依赖生产数据库；
- 能够重复创建和清理；
- 能够稳定复现测试结果。

协议测试样本应集中维护并复用。

涉及时间、随机数和网络行为时，应优先使用可控制的测试实现，避免测试结果依赖真实时间或随机状态。

## 4.12 测试编写要求

测试名称必须描述具体行为。

推荐：

    it('does not register duplicate listeners when initialized twice')

不推荐：

    it('works')

每个测试应尽量遵循：

    准备条件
    → 执行行为
    → 验证结果

测试必须验证对外行为，不应过度依赖内部实现细节。

禁止：

- 没有有效断言的测试；
- 只验证函数被调用，却不验证结果；
- 大量复制相同测试准备代码；
- 使用任意等待时间掩盖异步问题；
- 测试之间共享不可控的可变状态；
- 依赖执行顺序的测试。

## 4.13 测试完成检查表

    - [ ] 修改包含适当的单元测试
    - [ ] 跨模块修改包含集成测试
    - [ ] 关键链路修改评估了端到端测试
    - [ ] SDK 生命周期和销毁行为已验证
    - [ ] SDK 异常不会影响宿主页面
    - [ ] 插件不会重复监听或递归采集
    - [ ] 事件能够通过 event-schema 校验
    - [ ] Bug 修复包含回归测试
    - [ ] 异常和边界场景已经覆盖
    - [ ] 覆盖率满足对应模块基线
    - [ ] 核心真实浏览器测试通过
    - [ ] 测试数据不包含真实敏感信息
    - [ ] 不存在被静默忽略的不稳定测试

不适用的检查项必须说明原因。

## 4.14 初始测试规则

### TEST-001 v1：分层测试

Aurora 采用大量单元测试、适量集成测试和少量关键端到端测试。

### TEST-002 v1：SDK 职责测试

SDK Core 和采集插件必须覆盖正常流程、异常流程、边界和完整生命周期。

### TEST-003 v1：真实浏览器验证

模拟环境用于日常快速测试，真实浏览器用于验证关键浏览器能力和框架接入。

### TEST-004 v1：协议契约测试

SDK、数据接入和数据处理必须围绕 `event-schema` 使用共享契约测试。

### TEST-005 v1：风险优先覆盖率

Bug 修复原则上增加回归测试，关键模块采用较高覆盖率基线。

### TEST-006 v1：分级 CI 阻断

核心检查必须在 PR 阶段通过，完整兼容性、稳定性和性能测试在发布前通过。

## 规则维护与追加记录

### RULE-BASELINE-20260727：测试规范长期维护基线

- 状态：approved
- 生效日期：2026-07-27
- Owner：quality
- 影响范围：Aurora 全部测试、覆盖率、CI 和发布验证
- supersedes：none
- 维护方式：append-only
- 基线说明：本文件现有 TEST 规则构成 Aurora 初始测试基线。
- 本次校正：增加稳定元数据和关联文档；未改变既有测试原则。
- 历史保护：后续不得删除、覆盖或静默改写历史测试原则。新增、废弃或替代规则必须追加到本节之后。
- 验证方式：测试分层检查、覆盖率门槛、真实浏览器测试和 CI 阻断。
