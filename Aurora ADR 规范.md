---
title: Aurora ADR 规范
status: approved
owner: architecture
initial-reviewed: 2026-07-27
applies-to: Aurora 重大架构、公共协议、公共 API、基础设施、安全隐私及长期技术决策
related:
  - AURORA_RULES.md
  - Auroa-PRD-业务逻辑汇总-v2.1-核心业务定稿版.md
  - "Aurora 架构规范.md"
  - "Aurora 代码规范.md"
  - "Aurora 测试规范.md"
  - "Aurora 文档规范.md"
  - docs/adr/README.md
supersedes: none
maintenance: append-only
---

# 7. ADR 规范

> 规则类别：`ADR`  
> 适用范围：Aurora 的重大架构、公共协议、公共 API、基础设施、安全隐私及长期技术决策  
> 状态：已批准  
> 目标：记录重要技术决策的背景、方案取舍、实施约束和演进过程，使后续开发者能够理解 Aurora 为什么采用当前设计

## 关联文档

- [项目规则总入口](AURORA_RULES.md)
- [核心业务 PRD](Auroa-PRD-业务逻辑汇总-v2.1-核心业务定稿版.md)
- [架构规范](<Aurora 架构规范.md>)
- [代码规范](<Aurora 代码规范.md>)
- [测试规范](<Aurora 测试规范.md>)
- [文档规范](<Aurora 文档规范.md>)
- [初始 ADR 提案索引](docs/adr/README.md)

## 7.1 基本原则

Aurora 的 ADR 必须遵守以下原则：

- ADR 只记录长期影响较大、迁移成本较高的技术决策；
- 普通实现细节不得滥用 ADR；
- 正式实现原则上必须在 ADR 批准后开始；
- ADR 必须比较真实可行的候选方案；
- ADR 必须同时记录收益和代价；
- 只有 `accepted` 状态的 ADR 才是正式决策；
- 已批准 ADR 的结论不得静默改写；
- 决策变化时必须新增 ADR，并显式替代旧 ADR；
- ADR 批准不代表实施完成；
- ADR 的约束必须落实为代码、测试、工具检查或发布门槛；
- ADR、PRD、代码、测试和文档必须能够互相追溯。

## 7.2 ADR-001：创建边界

满足以下任一条件时，必须创建 ADR：

- 新增、删除或合并一级系统；
- 改变系统或模块职责；
- 改变模块依赖方向；
- 允许原本禁止的跨层依赖；
- 修改 SDK Core、Browser、采集插件和框架适配的分层；
- 将原本属于插件的能力并入 Core；
- 引入、替换或移除核心数据库；
- 引入、替换或移除消息队列；
- 引入、替换或移除核心缓存、搜索服务或对象存储；
- 将模块拆为独立服务；
- 将独立服务重新合并；
- 改变“同步接收、异步处理”的数据链路；
- 修改公共事件协议的兼容策略；
- 对公共 API 进行不兼容变更；
- 改变 SDK 默认采集原则；
- 改变安全、隐私或脱敏原则；
- 改变浏览器兼容承诺；
- 引入影响全项目的核心框架或工程基础设施；
- 决策实施后的迁移、回滚或替换成本明显较高；
- 存在多个合理方案，需要长期保留取舍依据。

以下情况通常不创建 ADR：

- 普通 Bug 修复；
- 不改变外部行为的局部重构；
- 私有函数命名；
- 普通文件拆分或移动；
- 不改变依赖方向的内部调整；
- 小型补丁版本依赖升级；
- 已批准架构内的普通功能实现；
- 没有长期技术影响的 UI 细节。

无法判断是否需要 ADR 时，应在技术方案中说明决策影响，再由评审者确定。

## 7.3 ADR-002：存放与命名

所有 ADR 必须存放在：

    docs/adr/

文件名格式：

    ADR-<三位编号>-<简短英文描述>.md

示例：

    ADR-001-use-monorepo.md
    ADR-002-sdk-plugin-architecture.md
    ADR-003-event-schema-source-of-truth.md
    ADR-004-database-task-buffer.md
    ADR-012-message-queue-buffer.md

规则：

- 编号按创建顺序递增；
- 编号一旦分配不得重复使用；
- 被拒绝、撤回或替代的 ADR 仍然占用原编号；
- 文件名不得使用 `new`、`final`、`latest`、`v2` 等状态后缀；
- 状态和替代关系通过 ADR 元数据表示。

## 7.4 ADR-003：标准模板

每份 ADR 必须采用以下基本模板：

    # ADR-XXX：决策标题

    ## 元数据

    - 状态：proposed
    - 日期：YYYY-MM-DD
    - Owner：
    - 适用范围：
    - 关联 PRD：
    - 关联技术方案：
    - 关联 Issue：
    - 关联实现 PR：
    - 替代 ADR：无
    - 被替代 ADR：无
    - 实施状态：not-started

    ## 背景

    当前遇到了什么问题，为什么需要作出长期决策。

    ## 决策驱动因素

    本次决策重点考虑哪些因素，例如：

    - SDK 对宿主页面的影响；
    - 性能和包体积；
    - 数据可靠性；
    - 协议兼容性；
    - 安全和隐私；
    - 运维复杂度；
    - 开发成本；
    - 当前业务规模；
    - 后续扩展能力。

    ## 候选方案

    ### 方案 A：名称

    方案说明。

    优点：

    - ...

    缺点：

    - ...

    ### 方案 B：名称

    方案说明。

    优点：

    - ...

    缺点：

    - ...

    ## 最终决策

    明确选择哪个方案，并说明最关键的理由。

    ## 结果与影响

    ### 正面影响

    - ...

    ### 负面影响与代价

    - ...

    ### 未解决问题

    - ...

    ## 实施约束

    后续代码、协议、模块或基础设施必须遵守的具体边界。

    ## 迁移方案

    如何从当前状态迁移到目标状态。

    ## 回滚方案

    实施失败时如何恢复。

    ## 验证方式

    如何证明决策达到了预期。

    ## 重新评估条件

    出现哪些情况时需要重新讨论本决策。

简单决策可以缩短篇幅，但不得省略：

- 背景；
- 候选方案；
- 最终决策；
- 决策理由；
- 负面影响；
- 实施约束；
- 验证方式。

## 7.5 候选方案要求

每份 ADR 原则上至少比较两个真实可行的方案。

禁止：

- 使用明显不可行的方案作为陪衬；
- 只写最终方案，不记录替代方案；
- 只记录优点，不记录代价；
- 仅使用“更好”“更先进”“更流行”等模糊理由；
- 为已经完成的实现倒推虚假的决策过程。

候选方案比较应围绕实际决策因素，例如：

- SDK 主线程开销；
- 宿主页面稳定性；
- 协议兼容；
- 数据可靠性；
- 部署和运维成本；
- 迁移复杂度；
- 团队当前能力；
- 当前和预期业务规模。

## 7.6 ADR-004：状态管理

ADR 允许使用以下状态：

| 状态         | 含义                         |
|--------------|------------------------------|
| `proposed`   | 已提出，正在讨论，尚未生效   |
| `accepted`   | 已批准，当前正式有效         |
| `rejected`   | 已评审但决定不采用           |
| `superseded` | 已被新的 ADR 替代            |
| `deprecated` | 仍可能存在，但不建议继续采用 |
| `withdrawn`  | 在形成正式决策前被撤回       |

状态规则：

- 只有 `accepted` ADR 才能作为正式开发依据；
- `proposed` ADR 不得约束正式实现；
- `rejected` ADR 必须保留拒绝原因；
- `superseded` ADR 必须链接到替代它的新 ADR；
- `deprecated` ADR 必须说明推荐的新方案；
- `withdrawn` ADR 必须保留撤回原因；
- 禁止删除已分配编号的 ADR。

## 7.7 ADR 审批

ADR 至少需要一名非作者评审并批准。

以下决策必须有对应领域评审：

- 安全决策：安全负责人或指定评审者；
- 隐私和数据采集决策：隐私或数据治理评审者；
- 公共协议决策：SDK、数据接入和数据处理相关评审者；
- 公共 API 不兼容变更：SDK 或 API 维护者；
- 数据库、队列和部署架构：服务端或基础设施维护者。

ADR 批准前可以进行：

- 技术调研；
- 性能实验；
- 可行性原型；
- 临时验证代码。

这些内容必须明确标记为实验或调研，不得直接作为正式实现合并。

## 7.8 决策先于正式实现

需要 ADR 的正式实现原则上不得早于 ADR 被标记为 `accepted`。

推荐流程：

    发现重大决策
    → 创建 ADR 草案
    → 比较候选方案
    → 完成评审
    → ADR accepted
    → 创建或确认实施任务
    → 编写正式代码
    → 完成验证
    → ADR implemented

禁止：

- 代码全部完成后才补写 ADR；
- 使用已完成代码迫使评审者接受方案；
- ADR 尚未批准就修改正式公共 API 或协议；
- 实际实现与 ADR 不一致但不更新决策记录。

线上紧急止损可以先执行最小修复，但故障恢复后必须补充必要 ADR。

## 7.9 ADR-005：替代与废弃

已经 `accepted` 的 ADR 不得直接修改最终结论。

决策发生变化时必须：

1.  创建新的 ADR；
2.  使用新的连续编号；
3.  在新 ADR 中说明替代的旧 ADR；
4.  解释旧方案为什么不再适用；
5.  将旧 ADR 状态修改为 `superseded`；
6.  在旧 ADR 顶部链接新 ADR；
7.  保留新旧 ADR；
8.  更新相关架构、代码、测试和迁移文档。

示例：

    ADR-004：使用数据库任务表作为可靠缓冲
    状态：superseded
    被 ADR-012 替代
    ADR-012：使用消息队列作为可靠缓冲
    状态：accepted
    替代 ADR-004

如果旧方案短期仍需维持，但不允许新增使用，可以将状态标记为 `deprecated`。

不得：

- 复用旧 ADR 编号；
- 删除被替代 ADR；
- 静默重写旧 ADR 的决策理由；
- 在同一 ADR 文件中维护多个相互冲突的当前结论。

允许直接修正的内容仅限于：

- 错别字；
- 无语义变化的格式；
- 失效链接；
- 明确错误的关联编号。

修正必须记录在 Git 历史中。

## 7.10 ADR-006：实施状态

ADR 的决策状态与实施状态必须分开管理。

允许的实施状态：

| 实施状态                | 含义                 |
|-------------------------|----------------------|
| `not-started`           | 已批准，尚未开始实施 |
| `in-progress`           | 正在实施             |
| `partially-implemented` | 仅完成部分内容       |
| `implemented`           | 已完成实施和约定验证 |
| `rolled-back`           | 实施后已经回滚       |

规则：

- ADR `accepted` 不代表已经实施；
- 标记为 `partially-implemented` 时必须列出未完成内容；
- ADR 标记为 `implemented` 前必须完成约定的验证；
- 实施后回滚时必须标记为 `rolled-back`，并说明原因；
- 回滚造成决策变化时，应评估是否需要新的 ADR。

## 7.11 决策—实施—验证闭环

每份 `accepted` ADR 必须关联：

- 相关 PRD；
- 相关技术方案；
- 实现 Issue；
- 实现 PR；
- 相关架构文档；
- 相关模块 README；
- 测试或验证方式；
- 必要迁移任务；
- 必要发布说明。

实现 PR 必须引用 ADR 编号，例如：

    ## 关联 ADR

    - ADR-012：使用消息队列作为事件可靠缓冲

评审者必须检查：

- 实现是否符合最终决策；
- 实施约束是否完整落实；
- 测试是否验证决策目标；
- 是否存在未经记录的偏离；
- 相关文档是否已经同步。

## 7.12 可执行约束

ADR 中的实施约束不能只停留在文字层面。

每项关键约束必须尽量转化为至少一种可执行措施：

- TypeScript 类型约束；
- ESLint 规则；
- Monorepo 依赖规则；
- 包导出边界；
- 运行时 Schema；
- 单元测试；
- 集成测试；
- 契约测试；
- 真实浏览器测试；
- CI 阻断；
- 发布检查；
- 运行时监控；
- 告警规则。

示例：

    ADR 决策：
    Core 不得直接访问浏览器对象。

    落地措施：
    - Core 的 TypeScript 配置不包含 DOM 类型；
    - ESLint 禁止 Core 使用 window 和 document；
    - CI 检查 Core 不依赖 Browser；
    - 环境能力通过适配接口注入；
    - 为环境适配接口增加单元测试。

再例如：

    ADR 决策：
    event-schema 是事件协议唯一来源。

    落地措施：
    - SDK 事件类型从 event-schema 导入；
    - 数据接入使用同一运行时 Schema；
    - 禁止跨包复制事件类型；
    - CI 执行共享契约测试；
    - 协议文档从 Schema 生成字段表。

## 7.13 临时例外

确实需要临时偏离已批准 ADR 时，必须记录：

- 偏离的 ADR；
- 偏离原因；
- 影响范围；
- 风险；
- 临时措施；
- 清理任务；
- Owner；
- 结束条件。

临时例外不得：

- 无限期存在；
- 扩大到未批准范围；
- 削弱安全或隐私底线；
- 被后续代码当作新的默认架构；
- 用于绕过应当创建的新 ADR。

如果临时例外长期存在，应创建新 ADR 重新决策。

## 7.14 重新评估

ADR 必须声明重新评估条件。

常见条件包括：

- 事件量超过预期阈值；
- SDK 包体积超过预算；
- 浏览器兼容范围变化；
- 队列积压达到不可接受水平；
- 基础设施成本显著增长；
- 当前方案出现持续稳定性问题；
- 隐私或安全要求变化；
- 公共 API 无法满足新需求；
- 团队规模或部署方式发生重大变化；
- 原方案依赖的技术停止维护。

达到重新评估条件不代表旧 ADR 自动失效。

必须通过新的评审或新 ADR 作出后续决定。

## 7.15 ADR 检查表

ADR 进入 `accepted` 前必须确认：

    - [ ] 决策属于需要 ADR 的范围
    - [ ] 背景和问题描述清楚
    - [ ] 决策驱动因素明确
    - [ ] 至少比较两个真实可行方案
    - [ ] 记录了各方案的优点和缺点
    - [ ] 最终决策明确
    - [ ] 记录了负面影响和代价
    - [ ] 实施约束可执行
    - [ ] 迁移和回滚方式明确
    - [ ] 验证方式明确
    - [ ] 重新评估条件明确
    - [ ] 关联 PRD 和技术方案
    - [ ] 至少一名非作者批准
    - [ ] 必要领域评审已经完成

ADR 标记为 `implemented` 前必须确认：

    - [ ] 实现 Issue 已关联
    - [ ] 实现 PR 已关联
    - [ ] 实现符合 ADR 决策
    - [ ] 关键约束已转化为自动检查或测试
    - [ ] 约定测试已经通过
    - [ ] 架构文档已更新
    - [ ] 模块 README 已更新
    - [ ] 迁移文档已更新
    - [ ] 没有未说明的实施偏差
    - [ ] 未完成内容已经清零或明确记录

## 7.16 初始 ADR 规则

### ADR-001 v1：按长期影响创建 ADR

只有会长期影响系统边界、公共协议、公共 API、基础设施、安全隐私或产生较高迁移成本的决策才必须创建 ADR。

### ADR-002 v1：标准 ADR 模板

ADR 必须记录背景、候选方案、最终决策、影响、实施约束、迁移、回滚和验证方式。

### ADR-003 v1：明确状态与审批

只有经过非作者及必要领域评审、状态为 `accepted` 的 ADR 才是正式决策。

### ADR-004 v1：新增 ADR 替代旧决策

已批准 ADR 不得静默改写；决策变化时必须新增 ADR，并显式替代旧 ADR。

### ADR-005 v1：决策实施验证闭环

每份已批准 ADR 必须关联实施任务和 PR，并将关键约束落实为代码、测试、自动检查或发布门槛。

## 规则维护与追加记录

### RULE-BASELINE-20260727：ADR 规范长期维护基线

- 状态：approved
- 生效日期：2026-07-27
- Owner：architecture
- 影响范围：Aurora 全部重大长期技术决策
- supersedes：none
- 维护方式：append-only
- 基线说明：本文件现有 ADR 规则构成 Aurora 初始决策治理基线。
- 本次校正：增加稳定元数据、关联文档和初始提案索引；未改变既有 ADR 原则。
- 实施门禁：`proposed` ADR 只用于讨论和评审，不得约束正式实现；只有 `accepted` ADR 才是正式决策。
- 历史保护：后续不得删除、覆盖或静默改写历史 ADR 原则。新增、废弃或替代规则必须追加到本节之后。
- 验证方式：ADR 状态、评审、候选方案、实现关联和验证闭环检查。
