# 5. 文档规范

> 规则类别：`DOC`  
> 适用范围：Aurora 规则文件、PRD、架构设计、ADR、模块 README、SDK 文档、API 文档、协议文档、测试文档、发布与运维文档  
> 状态：已批准  
> 目标：保证文档与代码同步，使开发者或新对话能够快速恢复准确的项目上下文

## 5.1 基本原则

Aurora 文档必须遵守以下原则：

- 文档即代码；
- 仓库中的 Markdown 是正式文档唯一来源；
- 文档必须与代码、协议和实际运行行为一致；
- 同一内容只维护一个权威版本；
- 模块文档靠近代码，跨模块设计集中存放；
- 公共 API、事件协议和兼容行为必须完整记录；
- 影响用户或系统行为的修改必须同步更新文档；
- 正式文档必须标明状态、适用范围和维护责任；
- 已被替代的规则和重要决策记录必须保留；
- 文档应帮助读者理解行为和约束，不应简单复制代码实现；
- 新对话必须按照固定顺序读取文档。

## 5.2 DOC-001：文档即代码

所有正式文档原文必须存放在 Aurora Monorepo 中，并采用 Markdown 格式。

文档网站、npm README 和其他展示渠道属于发布载体，不是独立权威来源。

文档流程为：

    仓库 Markdown
    → Git 版本控制
    → Pull Request 评审
    → 文档构建
    → 文档网站或 npm 页面发布

禁止：

- 只在文档网站后台修改长期有效内容；
- 在多个位置手工维护同一份完整文档；
- 使用聊天记录代替正式文档；
- 代码已经改变，但正式文档仍描述旧行为；
- 将未提交到仓库的个人笔记作为项目规范。

`AURORA_RULES.md` 是 Aurora 规则体系的最高级入口。

规则文件的历史维护继续遵守追加式原则：历史规则不得删除或静默覆盖，规则变化必须追加新版本和替代声明。

## 5.3 DOC-002：文档目录

跨模块和长期有效的正式文档统一存放在：

    docs/
    ├── prd/             # 产品需求、业务规则和验收标准
    ├── architecture/    # 系统架构、模块设计和数据流
    ├── adr/             # 架构决策记录
    ├── api/             # 服务端和管理平台公开 API
    ├── sdk/             # SDK 安装、接入、配置和公共 API
    ├── protocol/        # event-schema、协议版本和兼容策略
    ├── testing/         # 测试策略、矩阵、环境和执行说明
    ├── releases/        # 发布说明、升级说明和迁移指南
    ├── operations/      # 部署、监控、故障处理和恢复流程
    └── security/        # 安全、隐私、脱敏和数据治理

每个应用和包在自身目录维护模块 README：

    apps/console/README.md
    apps/server/README.md
    packages/core/README.md
    packages/browser/README.md
    packages/plugin-error/README.md
    packages/event-schema/README.md

目录职责：

- `docs` 保存跨模块、长期有效的正式设计；
- 模块 README 保存理解、开发和测试当前模块所需的信息；
- 代码注释解释局部实现原因；
- ADR 记录重要技术选择及其取舍；
- 发布文档记录用户可感知的版本变化。

禁止使用以下方式区分文档版本：

    architecture-new.md
    architecture-final.md
    architecture-final-v2.md
    api-latest.md
    sdk-copy.md

文档版本必须通过 Git、文档元数据和替代关系管理。

## 5.4 DOC-003：模块 README

每个 `apps/*` 和 `packages/*` 模块必须维护 `README.md`。

README 至少包含：

    # 模块名称

    ## 模块定位

    模块属于哪个一级系统，解决什么问题。

    ## 职责

    模块负责的能力。

    ## 非职责

    模块明确不处理的内容。

    ## 对外接口

    公开导出、API 或主要调用入口。

    ## 输入与输出

    模块接收什么数据，产生什么结果。

    ## 依赖边界

    允许依赖和禁止依赖的模块。

    ## 核心流程

    简要描述主要数据流和生命周期。

    ## 配置

    配置项、默认值和主要影响。

    ## 错误与生命周期

    初始化、重复初始化、停止、销毁和异常行为。

    ## 开发与测试

    本地开发命令、构建命令和主要测试范围。

    ## 关联文档

    相关 PRD、架构、ADR、协议和接入文档。

README 应保持简洁，不重复复制完整设计文档。详细内容必须链接到 `docs` 中的权威文档。

### SDK 包补充内容

SDK 相关包还必须说明：

- 支持的运行环境；
- 浏览器兼容范围；
- 是否进入用户最终产物；
- 包体积注意事项；
- 初始化与销毁行为；
- 对宿主页面的影响；
- 公共导出入口。

### 采集插件补充内容

采集插件还必须说明：

- 捕获哪些事件；
- 明确忽略哪些事件；
- 使用哪些浏览器 API；
- 生成哪种标准事件；
- 字段长度、数量和深度限制；
- 敏感字段处理；
- 如何避免递归采集；
- 如何停止监听并恢复原生 API。

### 服务端模块补充内容

服务端模块还必须说明：

- 对外或内部接口；
- 输入校验；
- 幂等性；
- 重试策略；
- 队列或存储依赖；
- 失败和死信处理；
- 可观测性要求。

### 管理平台模块补充内容

管理平台模块还必须说明：

- 路由入口；
- 权限要求；
- 依赖的公开 API；
- 加载、空数据、错误和无权限状态；
- 主要用户操作。

### Event Schema 补充内容

`event-schema` README 还必须说明：

- 当前协议版本；
- 支持的事件类型；
- 运行时校验方式；
- 兼容性规则；
- 契约测试样本位置；
- 协议变更流程。

## 5.5 DOC-004：SDK 文档

SDK 接入文档必须至少包含：

- 支持的安装方式；
- 包名和版本要求；
- 最小初始化示例；
- 完整配置项；
- 配置类型；
- 默认值；
- 取值范围；
- 插件启用方式；
- 框架接入方式；
- 重复初始化行为；
- `flush` 和 `destroy` 等生命周期行为；
- 采样语义；
- 缓存和批量上报行为；
- 失败重试和丢弃行为；
- 浏览器支持范围；
- Vue、React 等框架支持范围；
- 默认采集的数据；
- 默认不采集的数据；
- 隐私和脱敏注意事项；
- 常见错误和排查方式；
- 升级和迁移方式。

SDK 文档不得承诺代码尚未实现的能力。

实验性能力必须明确标记：

- 实验状态；
- 不保证长期兼容；
- 可能发生的变化；
- 是否建议生产使用。

## 5.6 公共 SDK API 文档

每个公共 SDK API 必须说明：

- 名称；
- 函数签名；
- 参数；
- 返回值；
- 默认行为；
- 同步或异步；
- 是否允许重复调用；
- 调用前置条件；
- 生命周期影响；
- 失败或异常行为；
- 最小示例；
- 兼容性状态；
- 废弃状态。

示例结构：

    ## captureException

    ### 签名

    ```ts

    captureException(error: unknown, context?: ErrorContext): void

### 参数

- `error`：需要记录的异常。
- `context`：可选的附加上下文。

### 行为

将异常转换为标准错误事件，并进入统一事件处理管道。

### 失败行为

SDK 内部处理失败时不会向宿主页面抛出异常。

### 示例


    aurora.captureException(error)

### 兼容性

自 `1.0.0` 起提供。



    废弃 API 必须说明：

    - 废弃版本；
    - 替代 API；
    - 迁移示例；
    - 计划移除版本；
    - 行为差异。

    ---

    ## 5.7 服务端 API 文档

    所有公开服务端 API 必须记录：

    - 请求方法；
    - 请求路径；
    - 用途；
    - 鉴权方式；
    - 权限要求；
    - 路径参数；
    - 查询参数；
    - 请求体；
    - 响应结构；
    - 错误码；
    - 分页；
    - 排序；
    - 过滤；
    - 幂等性；
    - 限流规则；
    - 示例请求；
    - 示例响应；
    - API 版本；
    - 兼容性说明。

    示例中禁止使用真实 Token、Cookie、项目密钥或用户数据。

    管理平台不得依赖未记录的服务端内部接口。

    ---

    ## 5.8 事件协议文档

    `docs/protocol` 必须与 `packages/event-schema` 保持一致。

    事件协议文档至少包含：

    - 当前协议版本；
    - 支持的历史版本；
    - 上报批次结构；
    - 事件公共字段；
    - 各事件专属字段；
    - 必填字段；
    - 可选字段；
    - 默认值；
    - 枚举值；
    - 字符串长度限制；
    - 数组数量限制；
    - 对象深度限制；
    - 时间戳要求；
    - 禁止字段；
    - 脱敏规则；
    - 合法样本；
    - 非法样本；
    - 旧版本兼容方式；
    - 协议升级和迁移方式。

    协议字段表可以由 TypeScript 类型或运行时 Schema 自动生成。

    以下内容必须人工维护：

    - 字段业务含义；
    - 隐私影响；
    - 失败语义；
    - 兼容策略；
    - 迁移说明；
    - 设计取舍。

    协议文档和运行时 Schema 不一致时，以已批准的协议设计和 Schema 为排查依据，但必须立即修复不一致，不能长期保留两个定义。

    ---

    ## 5.9 文档示例

    文档中的代码和配置示例必须可验证。

    至少采用以下一种方式：

    - TypeScript 类型检查；
    - 自动化文档示例测试；
    - 示例项目编译；
    - 契约测试；
    - CI 中执行命令验证。

    禁止：

    - 提供无法编译的示例；
    - 使用已经废弃的 API；
    - 使用不存在的配置项；
    - 省略会改变结果的必要配置；
    - 使用真实敏感信息；
    - 将伪代码标记为可直接运行代码。

    伪代码必须明确标记为“示意代码”。

    `examples` 下的正式示例项目必须能够在 CI 或发布前验证。

    ---

    ## 5.10 DOC-005：文档同步

    以下变化必须在同一个 PR 中更新相关文档。

    ### 产品和用户行为变化

    包括：

    - 新功能；
    - 用户操作流程变化；
    - 权限和可见范围变化；
    - 验收标准变化；
    - 已确认限制或非目标变化。

    需要更新：

    - PRD；
    - 用户操作文档；
    - 管理平台说明；
    - 必要的模块 README。

    ### SDK 公共能力变化

    包括：

    - 新增、删除或重命名公共 API；
    - 参数和返回值变化；
    - 默认行为变化；
    - 配置项变化；
    - 插件启用方式变化；
    - 初始化、重复初始化、`flush` 或 `destroy` 行为变化；
    - 浏览器和框架兼容范围变化。

    需要更新：

    - SDK 接入文档；
    - 公共 API 文档；
    - 模块 README；
    - 发布说明；
    - 必要的迁移指南。

    ### 数据采集和隐私变化

    包括：

    - 新增采集类型；
    - 新增采集字段；
    - 默认采集行为变化；
    - 脱敏或过滤规则变化；
    - 采样策略变化；
    - 字段长度和数量限制变化；
    - 数据保留和删除策略变化。

    需要更新：

    - SDK 文档；
    - 协议文档；
    - 安全与隐私文档；
    - 必要的 PRD 和 ADR。

    ### 协议和服务端 API 变化

    包括：

    - `event-schema` 字段或版本变化；
    - 查询 API 结构变化；
    - 错误码变化；
    - 鉴权变化；
    - 分页或限流变化；
    - 兼容策略变化。

    需要更新：

    - 协议文档；
    - API 文档；
    - 契约测试样本；
    - 发布和迁移文档；
    - 必要的 ADR。

    ### 架构和运行方式变化

    包括：

    - 模块职责变化；
    - 依赖方向变化；
    - 新增应用或包；
    - 新增队列、数据库或服务；
    - 数据处理流程变化；
    - 部署和故障处理方式变化。

    需要更新：

    - 架构文档；
    - 模块 README；
    - ADR；
    - 运维文档。

    ### 测试和质量门槛变化

    包括：

    - 浏览器测试矩阵变化；
    - 覆盖率基线变化；
    - CI 阻断条件变化；
    - 测试命令或测试环境变化。

    需要更新：

    - 测试文档；
    - 模块 README；
    - 发布检查说明。

    不改变公开行为、协议、模块职责和运行方式的内部重构通常不需要更新正式文档。

    但每个 PR 都必须填写：

    ```markdown
    ## 文档影响

    - [ ] 无文档影响
    - [ ] 已更新模块 README
    - [ ] 已更新 SDK 文档
    - [ ] 已更新 API 或协议文档
    - [ ] 已更新 PRD
    - [ ] 已更新架构或 ADR
    - [ ] 已更新测试文档
    - [ ] 已更新发布或运维文档

    说明：

禁止使用没有负责人和期限的“后续再补文档”代替必要的同步更新。

线上紧急故障可以暂时延后非关键文档，但必须在事故补档阶段完成。

## 5.11 DOC-006：文档状态与元数据

每份正式文档顶部必须声明元数据。

推荐格式：

    ---
    title: 文档标题
    status: draft
    owner: sdk
    last-reviewed: 2026-07-27
    applies-to: Aurora SDK 1.x
    related:
      - ../adr/ADR-001-example.md
    supersedes: none
    ---

### 状态

允许的文档状态：

| 状态         | 含义                       |
|--------------|----------------------------|
| `draft`      | 草案，仅供讨论             |
| `approved`   | 已批准，当前有效           |
| `deprecated` | 仍可参考，但不建议继续使用 |
| `superseded` | 已被其他文档替代           |
| `archived`   | 仅供历史追溯               |

状态规则：

- `draft` 不得作为正式开发依据；
- `approved` 才能作为当前正式设计依据；
- `deprecated` 必须说明推荐替代方案；
- `superseded` 必须链接到新文档；
- 被替代文档不得直接删除；
- 历史 PRD、ADR、规则和迁移记录必须保留。

### Owner

文档责任必须归属于模块或角色，例如：

    sdk
    platform
    backend
    architecture
    security
    release

不应长期绑定某一个具体个人。

修改模块行为的开发者负责同步更新该模块相关文档。

Owner 负责保证文档长期准确，但不代表只有 Owner 可以修改文档。

### Last Reviewed

`last-reviewed` 表示最近一次确认文档仍然准确的日期，不等于最后一次文字修改时间。

以下时间必须复查相关文档：

- 正式版本发布前；
- 公共 API 变化后；
- 协议变化后；
- 架构变化后；
- 安全或隐私变化后；
- P0、P1 事故复盘后；
- 新成员或新对话无法正确理解项目时。

超过维护周期未复查的文档应标记“待复查”，但不会自动失效。

## 5.12 文档命名

正式文档文件名使用小写 `kebab-case`。

示例：

    sdk-getting-started.md
    event-protocol-v1.md
    error-processing-architecture.md
    browser-compatibility.md
    incident-response.md

ADR 使用固定编号：

    ADR-001-monorepo.md
    ADR-002-event-schema.md

PRD 可以使用稳定编号：

    PRD-001-project-management.md
    PRD-002-error-monitoring.md

禁止在文件名中使用：

    final
    new
    latest
    copy
    正式版
    最终版

文档当前状态由元数据和 Git 历史表达。

## 5.13 文档链接

文档之间应使用相对路径链接。

重要文档必须建立双向关联，例如：

- PRD 链接相关 ADR；
- ADR 链接相关架构文档；
- SDK 文档链接公共 API 和协议；
- 发布说明链接迁移指南；
- 模块 README 链接正式设计；
- 测试文档链接兼容矩阵。

文档移动时必须同步修复引用。

CI 应检查：

- 失效内部链接；
- 不存在的文件路径；
- Markdown 基本格式；
- 可执行代码示例；
- 必填元数据。

## 5.14 新对话读取顺序

新的开发对话或新成员进入项目时，按照以下顺序读取：

    1. AURORA_RULES.md
    2. 最新的项目上下文快照
    3. 当前任务对应的 PRD
    4. 相关的已批准 ADR
    5. 涉及模块的 README
    6. 相关 SDK、API 或协议文档
    7. 相关测试、发布或运维文档

不得默认要求每次读取整个 `docs` 目录。

`AURORA_RULES.md` 必须维护“当前必读文档索引”，至少包含：

    ## 当前必读文档

    | 文档 | 路径 | 状态 | 用途 |
    |---|---|---|---|
    | 当前 PRD | docs/prd/... | approved | 当前功能范围 |
    | 核心架构 | docs/architecture/... | approved | 系统边界 |
    | 相关 ADR | docs/adr/... | approved | 关键技术决策 |
    | SDK 文档 | docs/sdk/... | approved | 接入与公共行为 |

开始任务前，应确认读取的是当前有效文档，而不是已废弃或已被替代的版本。

## 5.15 文档质量要求

正式文档必须：

- 标题明确；
- 范围明确；
- 术语统一；
- 规则可验证；
- 示例可运行；
- 链接有效；
- 说明默认行为；
- 说明失败行为；
- 说明不支持的范围；
- 说明兼容性；
- 避免无意义重复。

禁止使用模糊表达：

    尽量
    一般来说
    视情况而定
    应该没问题
    以后优化
    暂时这样

确实存在条件差异时，必须说明具体条件和处理方式。

## 5.16 文档检查表

代码或设计变更合并前必须确认：

    - [ ] 已判断本次变更的文档影响
    - [ ] 相关模块 README 仍然准确
    - [ ] 公共 API 文档已同步
    - [ ] SDK 配置和默认行为已同步
    - [ ] API 和协议字段已同步
    - [ ] 隐私和数据采集说明已同步
    - [ ] 浏览器兼容范围已同步
    - [ ] 测试命令和质量门槛已同步
    - [ ] 必要 ADR 已创建或更新
    - [ ] 发布或迁移说明已更新
    - [ ] 示例代码能够编译或运行
    - [ ] 文档元数据完整
    - [ ] 内部链接有效
    - [ ] 未重复维护同一份权威内容

不适用的检查项必须说明原因。

## 5.17 初始文档规则

### DOC-001 v1：文档即代码

Aurora 正式文档必须以 Monorepo 中的 Markdown 为唯一来源。

### DOC-002 v1：用途分类与模块 README

跨模块文档按用途存放于 `docs`，应用和包在自身目录维护 README。

### DOC-003 v1：统一 README 模板

模块 README 必须说明职责、非职责、接口、依赖、生命周期、测试及关联文档。

### DOC-004 v1：完整公共契约文档

SDK、服务端 API 和事件协议必须完整记录公开行为、限制、兼容性和迁移方式。

### DOC-005 v1：文档随变更同步

影响行为、公共接口、协议、隐私、架构和质量门槛的修改必须同步更新文档。

### DOC-006 v1：统一状态和读取顺序

正式文档必须声明状态、责任和替代关系；新对话按照固定顺序恢复项目上下文。
