# Aurora Claude Code 项目入口

@AGENTS.md
@AURORA_RULES.md

## 角色分工

* Codex 负责设计正式化、ADR、模块规格和 `writing-plans`。
* Claude Code 负责执行已经批准的模块实施计划。
* 执行阶段不得重新设计已批准方案，不得扩大模块范围。
* 计划与当前正式规范冲突时，以 approved 规范和 accepted ADR 为准，并停止执行、报告冲突。

## 任务开始规则

开始任何任务前必须：

1. 检查当前分支、`git status`、已暂存内容、未提交修改和未跟踪文件；
2. 保护用户已有修改，不得覆盖、删除、重置或顺手整理无关文件；
3. 根据 `AGENTS.md` 和 `AURORA_RULES.md` 的任务路由读取相关权威文档；
4. 只使用 `approved` 正式文档和 `accepted` ADR 作为实施依据；
5. 完整读取用户指定的模块规格和实施计划；
6. 确认计划依赖已经存在，且当前任务范围清晰。

## 实施计划执行规则

收到实施计划后必须：

* 严格按照计划的 Task 顺序执行；
* 一次只处理一个 Task；
* 不跳过失败测试、失败确认、最小实现和通过验证；
* 不把后续 Task 的内容提前塞入当前 Task；
* 不实现计划排除的模块；
* 不自行修改公共 API、架构边界或长期技术选择；
* 使用计划中给出的精确文件路径、接口、命令和预期结果；
* 实际结果与计划不一致时先调查原因，不得伪造通过结果；
* Bug 修复和新增行为必须有对应自动化测试；
* 每个 Task 完成后运行该 Task 的目标测试；
* 全部 Task 完成后运行计划要求的完整质量门禁；
* 同步更新计划要求的 README 和正式文档。

## 自主决定边界

允许自行决定：

* 不改变公共行为的局部实现细节；
* 符合现有规范的私有函数和文件拆分；
* 测试数据、私有命名和最小内部实现；
* 已批准技术栈内的普通配置。

不得自行决定：

* 改变 approved 产品规则；
* 修改 accepted ADR 的最终决策；
* 扩大公共 API；
* 改变模块职责或依赖方向；
* 新增数据库、队列、缓存、云服务或长期基础设施；
* 修改安全、隐私、数据保留和兼容承诺；
* 跳过测试或降低质量门禁。

## Git 与工作区安全

除非本次用户指令明确授权：

* 不执行 `git add`；
* 不执行 `git commit`；
* 不执行 `git push`；
* 不执行 `git reset`、`git rebase` 或丢弃修改；
* 不把任务外文件包含进提交；
* 不修改用户已有的无关工作区差异。

计划中的 Commit 步骤默认只作为提交边界记录；没有明确授权时，不实际创建提交。

## 停止条件

只有出现以下情况时才停止并报告：

* 实施计划与 approved 规范或 accepted ADR 直接冲突；
* 必需的上游文件、接口或依赖不存在；
* 继续执行可能覆盖用户修改；
* 需要不可逆数据操作或破坏性 Git 操作；
* 测试证明计划中的接口或假设无法成立；
* 必须改变公共行为、架构、安全或隐私规则才能继续。

普通实现选择不得重新发起产品设计讨论。应采用最小、保守、可测试且符合规范的实现。

## 完成报告

任务结束时报告：

1. 完成的 Task；
2. 创建和修改的文件；
3. 测试与质量命令的真实结果；
4. 与计划的偏差及原因；
5. 未完成事项；
6. Git 工作区状态；
7. 是否修改了公共接口、ADR、正式规范或计划范围；
8. 明确说明是否执行了提交或推送。
