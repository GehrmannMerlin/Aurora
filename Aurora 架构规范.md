---
title: Aurora 架构规范
status: approved
owner: architecture
initial-reviewed: 2026-07-27
applies-to: Aurora Monorepo、SDK、服务端、管理平台及公共协议
related:
  - AURORA_RULES.md
  - Auroa-PRD-业务逻辑汇总-v2.1-核心业务定稿版.md
  - "Aurora 代码规范.md"
  - "Aurora 测试规范.md"
  - "Aurora 文档规范.md"
  - "Aurora ADR 规范.md"
  - docs/adr/README.md
supersedes: none
maintenance: append-only
---

# 2. 架构规范

> 规则类别：`ARCH`  
> 适用范围：Aurora Monorepo、SDK、数据接入、数据处理、管理平台及公共协议  
> 状态：已批准  
> 目标：明确系统边界、模块职责和依赖方向，防止项目随着功能增长形成循环依赖和职责混乱

## 关联文档

- [项目规则总入口](AURORA_RULES.md)
- [核心业务 PRD](Auroa-PRD-业务逻辑汇总-v2.1-核心业务定稿版.md)
- [代码规范](<Aurora 代码规范.md>)
- [测试规范](<Aurora 测试规范.md>)
- [文档规范](<Aurora 文档规范.md>)
- [ADR 规范](<Aurora ADR 规范.md>)
- [初始 ADR 提案索引](docs/adr/README.md)

## 2.1 架构原则

Aurora 必须遵守以下架构原则：

- 采用统一 Monorepo 管理所有代码；
- 系统按照职责划分，而不是按照页面或开发人员划分；
- SDK、服务端和管理平台只能通过公开接口或公共协议通信；
- 依赖必须保持单向；
- 底层模块不得依赖上层业务模块；
- 事件协议必须只有一个权威来源；
- SDK 插件只负责采集，不自行实现完整上报链路；
- 数据接入链路保持轻量，复杂处理异步执行；
- 架构边界必须通过工具检查，不能只依赖开发者自觉；
- 项目早期允许多个模块部署在同一应用中，但代码边界不得因此合并。

## 2.2 ARCH-001：统一 Monorepo

Aurora 的 SDK、服务端、管理平台、公共协议、文档和示例项目统一放在一个仓库中。

推荐目录结构：

    aurora/
    ├── apps/
    │   ├── console/                 # 管理平台
    │   ├── server/                  # 服务端应用
    │   └── docs/                    # 文档站点
    │
    ├── packages/
    │   ├── core/                    # SDK 核心
    │   ├── browser/                 # 浏览器环境适配
    │   ├── plugin-error/            # 错误监控插件
    │   ├── plugin-performance/      # 性能监控插件
    │   ├── plugin-request/          # 请求监控插件
    │   ├── plugin-behavior/         # 用户行为插件
    │   ├── vue/                     # Vue 适配
    │   ├── react/                   # React 适配
    │   ├── event-schema/            # 公共事件协议
    │   └── shared/                  # 少量稳定基础能力
    │
    ├── examples/                    # 接入示例
    ├── docs/                        # 项目设计和规则文档
    └── tooling/                     # 构建、Lint 和工程工具

实际目录名称可以调整，但必须保持以下一级分类：

- 可部署应用放入 `apps`；
- 可复用包放入 `packages`；
- 示例项目放入 `examples`；
- 架构、ADR 和开发文档放入 `docs`；
- 工程工具放入独立工具目录。

禁止因为代码位于同一仓库，就绕过模块公开接口直接访问其他模块内部实现。

## 2.3 ARCH-002：五大系统边界

Aurora 分为五个一级系统。

### 2.3.1 SDK 系统

SDK 运行在接入 Aurora 的业务页面中，负责：

- 捕获浏览器错误；
- 捕获资源和请求异常；
- 采集性能指标；
- 采集必要的用户行为；
- 将原始数据转换为标准事件；
- 执行过滤、脱敏和采样；
- 缓存和批量上报事件。

SDK 禁止承担：

- 服务端数据聚合；
- 错误趋势分析；
- 告警计算；
- 平台查询；
- 用户权限判断；
- 服务端 Source Map 解析。

SDK 的首要目标是不能破坏宿主业务页面。

### 2.3.2 数据接入系统

数据接入系统负责：

- 接收 SDK 上报；
- 项目身份和接入凭证校验；
- 请求限流；
- 请求大小限制；
- 协议版本识别；
- 事件结构校验；
- 基础非法数据过滤；
- 将合法数据写入可靠缓冲区；
- 快速返回接收结果。

数据接入系统禁止在同步请求中执行耗时的数据分析、聚合或 Source Map 解析。

### 2.3.3 数据处理与存储系统

数据处理与存储系统负责：

- 消费待处理事件；
- 数据标准化；
- 数据清洗；
- 错误去重和分组；
- Source Map 解析；
- 性能指标聚合；
- 数据存储；
- 查询索引；
- 数据保留和清理；
- 告警规则计算。

该系统不得依赖 SDK 或管理平台的内部实现。

### 2.3.4 管理平台系统

管理平台负责：

- 项目管理；
- 接入配置；
- 错误列表与错误详情；
- 性能数据查看；
- 请求和行为数据查看；
- 告警配置；
- Source Map 管理；
- 成员与权限管理。

管理平台必须通过服务端公开 API 获取和修改数据。

管理平台禁止：

- 直接访问数据库；
- 直接消费内部消息队列；
- 依赖服务端私有代码；
- 自行解释未经服务端确认的原始事件。

### 2.3.5 公共协议系统

公共协议系统负责：

- 事件基础结构；
- 各类监控事件类型；
- 上报批次结构；
- 公共枚举；
- 协议版本；
- 运行时校验规则；
- 协议兼容性规则。

公共协议系统是 SDK、数据接入、数据处理和管理平台共同遵守的唯一协议来源。

## 2.4 ARCH-003：SDK 分层

Aurora SDK 采用“核心层、环境层、插件层、框架适配层”的结构。

    框架适配层
        ↓
    采集插件层
        ↓
    Browser 环境层
        ↓
    Core 核心层
        ↓
    Event Schema

箭头表示允许依赖的方向。

### 2.4.1 Core 核心层

Core 负责：

- SDK 生命周期；
- 配置管理；
- 插件注册；
- 事件管道；
- 事件处理钩子；
- 采样；
- 队列；
- 上报调度；
- 公共 SDK 接口。

Core 必须保持运行环境无关。

Core 禁止直接访问：

    window
    document
    navigator
    location
    XMLHttpRequest
    fetch
    localStorage
    sessionStorage

需要使用运行环境能力时，必须通过环境适配接口注入。

### 2.4.2 Browser 环境层

Browser 层负责封装浏览器能力：

- 页面地址；
- User Agent；
- 页面生命周期；
- 网络请求；
- 页面卸载上报；
- 浏览器存储；
- 时间和性能接口；
- 全局错误监听能力。

Browser 层向 Core 提供标准化环境接口，不得将浏览器细节扩散到 Core。

### 2.4.3 采集插件层

错误、性能、请求、行为和白屏等能力必须以插件形式存在。

插件负责：

- 注册监听；
- 捕获原始数据；
- 将数据转换为统一事件；
- 在停止时释放监听和资源。

插件禁止：

- 直接发送网络请求；
- 自行维护独立上报队列；
- 绕过统一采样；
- 绕过统一脱敏；
- 修改 Core 私有状态；
- 直接依赖其他采集插件的内部实现。

插件产生的事件必须进入统一事件管道。

### 2.4.4 框架适配层

Vue、React 等框架适配包仅负责：

- 接入框架生命周期；
- 捕获框架特有错误；
- 获取组件和路由上下文；
- 将框架信息转换为标准事件；
- 提供符合框架习惯的初始化接口。

框架适配包不得复制 Core、Browser 或插件层已有能力。

## 2.5 ARCH-004：数据异步处理

Aurora 的数据上报采用“同步接收、异步处理”模型。

    SDK
      ↓
    数据接入层
      ↓
    鉴权、限流、协议校验
      ↓
    可靠队列或缓冲区
      ↓
    快速返回已接收
      ↓
    异步数据处理
      ↓
    清洗、分组、聚合、Source Map 解析和存储

数据接入接口返回成功，只表示事件已被可靠接收，不表示事件已经完成处理或可以立即查询。

项目早期可以使用：

- 数据库任务表；
- 进程内队列加持久化；
- 轻量消息队列。

但必须满足：

- 接收成功的数据不能因进程重启直接丢失；
- 处理失败可以重试；
- 重复消费不会产生错误结果；
- 队列积压可以被监控；
- 无法处理的数据可以进入死信或失败记录。

禁止让 SDK 等待以下操作完成：

- Source Map 解析；
- 错误分组；
- 性能聚合；
- 告警计算；
- 复杂数据库查询。

## 2.6 ARCH-005：公共事件协议

Monorepo 中必须建立独立的公共协议包：

    packages/event-schema

该包是事件协议的唯一权威来源。

### 2.6.1 协议包内容

协议包可以包含：

- TypeScript 类型；
- 运行时 Schema；
- 事件枚举；
- 协议版本；
- 字段限制；
- 默认值规则；
- 兼容性转换函数；
- 测试数据生成器。

协议包不得包含：

- SDK 采集逻辑；
- 网络发送逻辑；
- 数据库访问；
- 平台页面逻辑；
- 服务端业务流程。

### 2.6.2 运行时校验

服务端不能只依赖 TypeScript 类型。

所有来自 SDK 的数据都必须被视为外部不可信输入，并通过运行时 Schema 校验。

运行时校验至少包括：

- 事件类型是否合法；
- 必填字段是否存在；
- 字段类型是否正确；
- 字符串长度是否超限；
- 数组数量是否超限；
- 时间戳是否合理；
- 协议版本是否受支持；
- 是否包含明确禁止的字段。

### 2.6.3 协议兼容

协议变更必须遵循：

- 新增可选字段通常视为兼容变更；
- 删除字段属于不兼容变更；
- 修改字段含义属于不兼容变更；
- 修改字段类型属于不兼容变更；
- 将可选字段改为必填字段属于不兼容变更；
- 修改枚举已有值的含义属于不兼容变更。

不兼容协议变更必须创建 ADR，并提供迁移和旧版本处理方案。

## 2.7 ARCH-006：单向依赖

Aurora 的核心依赖方向如下：

    event-schema
        ↑
    core
        ↑
    browser
        ↑
    采集插件和框架适配

服务端依赖方向如下：

    event-schema
        ↑
    数据接入
        ↓
    可靠队列
        ↓
    数据处理
        ↓
    数据存储
        ↓
    查询服务
        ↓
    管理平台 API
        ↓
    管理平台

依赖图只表示逻辑方向。实际代码可以通过接口和事件解耦，但不得产生反向依赖。

### 2.7.1 允许的依赖

- SDK 包可以依赖 `event-schema`；
- Browser 可以依赖 Core；
- 插件可以依赖公开的 Core 和 Browser 接口；
- 框架适配包可以依赖 SDK 公开接口；
- 数据接入和数据处理可以依赖 `event-schema`；
- 管理平台可以依赖公共前端类型和服务端公开 API 客户端。

### 2.7.2 禁止的依赖

禁止：

- Core 依赖 Browser；
- Core 依赖具体插件；
- Core 依赖 React 或 Vue；
- 插件直接访问 Core 私有文件；
- 插件之间直接依赖内部实现；
- 数据处理依赖数据接入的私有实现；
- 服务端依赖 SDK 内部代码；
- 管理平台直接依赖数据库模型；
- 管理平台直接访问数据库；
- 公共协议包依赖任何业务模块；
- 通过相对路径跨包访问其他包源码；
- 引用其他包的 `src`、`internal` 或私有目录；
- 任何形式的循环依赖。

跨包调用必须通过包的公开导出入口。

正确示例：

    import { ErrorEventSchema } from '@aurora/event-schema'

禁止示例：

    import { ErrorEventSchema } from '../../event-schema/src/error/internal'

## 2.8 Shared 包限制

`shared` 包只能用于真正稳定、无业务倾向并被多个系统复用的基础能力。

适合放入 `shared` 的内容：

- 通用结果类型；
- 无业务含义的工具函数；
- 稳定的日志接口；
- 通用时间和 ID 接口。

禁止放入：

- 仅被一个模块使用的函数；
- 数据库模型；
- SDK 插件逻辑；
- 页面业务逻辑；
- 临时常量；
- 为解决循环依赖而移动的代码；
- 无明确归属的杂项代码。

新增 `shared` 内容前必须先判断该能力是否应该归属于某个明确业务模块。

`shared` 不得成为默认代码存放位置。

## 2.9 模块职责约束

每个应用和包必须在自己的 README 或模块说明中声明：

- 模块职责；
- 对外公开接口；
- 允许依赖的模块；
- 禁止依赖的模块；
- 输入；
- 输出；
- 错误处理方式；
- 主要测试范围。

当一个模块同时承担多个不相关职责时，应优先拆分，而不是继续增加内部目录。

新增模块必须满足：

- 有独立且稳定的职责；
- 不能合理归入现有模块；
- 有明确输入和输出；
- 有明确依赖方向；
- 可以独立测试；
- 不会仅为一个函数或一个文件创建新包。

## 2.10 架构自动检查

Monorepo 必须通过自动化工具检查：

- 循环依赖；
- 禁止的跨包依赖；
- 私有目录引用；
- 未声明的包依赖；
- 浏览器代码进入 Core；
- 服务端代码进入 SDK；
- 管理平台直接依赖服务端内部模块。

检查应在本地开发和 CI 中执行。

架构检查失败时不得合并代码。

不得长期使用忽略规则绕过架构检查。确需临时忽略时，必须说明原因并创建清理任务。

## 2.11 必须创建 ADR 的架构变更

以下变更必须创建 ADR：

- 新增或删除一级系统；
- 改变一级系统职责；
- 改变模块依赖方向；
- 修改 SDK 核心分层；
- 将插件能力并入 Core；
- 新增长期运行的基础设施；
- 引入或替换消息队列；
- 引入或替换核心数据库；
- 将模块拆为独立服务；
- 将独立服务重新合并；
- 修改公共事件协议的兼容策略；
- 修改同步接收、异步处理模型；
- 允许原本禁止的跨层依赖。

普通文件移动、内部函数拆分和不改变依赖方向的局部重构，不要求创建 ADR。

## 2.12 架构检查表

涉及新模块或跨模块修改时必须确认：

    - [ ] 修改属于正确的系统和模块
    - [ ] 没有新增反向依赖
    - [ ] 没有产生循环依赖
    - [ ] 没有跨包引用私有文件
    - [ ] Core 未直接访问浏览器对象
    - [ ] 插件未绕过统一事件管道
    - [ ] 管理平台未直接访问数据库
    - [ ] 事件结构来自 event-schema
    - [ ] 外部事件经过运行时校验
    - [ ] 数据接入链路未加入复杂处理
    - [ ] 新增模块拥有明确职责
    - [ ] 重大架构变化已创建 ADR

## 2.13 初始架构决策

### ARCH-001 v1：统一 Monorepo

Aurora 的 SDK、服务端、管理平台、公共协议、文档和示例项目必须在统一 Monorepo 中管理。

### ARCH-002 v1：五大系统边界

Aurora 划分为 SDK、数据接入、数据处理与存储、管理平台和公共协议五个一级系统。

### ARCH-003 v1：SDK 核心分层与插件架构

SDK 必须采用 Core、Browser、采集插件和框架适配分层。

### ARCH-004 v1：同步接收、异步处理

数据接入层仅完成必要校验和可靠接收，复杂数据处理必须异步执行。

### ARCH-005 v1：公共协议唯一来源

事件类型、上报结构和运行时 Schema 必须由独立 `event-schema` 包统一管理。

### ARCH-006 v1：单向依赖和自动限制

模块依赖必须保持单向，并通过自动化工具阻止循环依赖、反向依赖和私有文件引用。

## 2.14 初始 ADR 提案索引

ARCH-001 至 ARCH-006 已分别形成可独立评审的 ADR 提案，见 [初始 ADR 提案索引](docs/adr/README.md)。

这些 ADR 的决策状态均为 `proposed`、实施状态均为 `not-started`。在完成正式评审并变更为 `accepted` 前，它们只用于记录候选方案和取舍，不构成新的实施批准，也不替代本架构规范。

## 规则维护与追加记录

### RULE-BASELINE-20260727：架构规范长期维护基线

- 状态：approved
- 生效日期：2026-07-27
- Owner：architecture
- 影响范围：Aurora 全部系统边界、模块职责和依赖方向
- supersedes：none
- 维护方式：append-only
- 基线说明：本文件现有 ARCH 规则构成 Aurora 初始架构基线。
- 本次校正：增加稳定元数据、关联文档和初始 ADR 提案索引；未改变既有架构原则。
- 历史保护：后续不得删除、覆盖或静默改写历史架构原则。新增、废弃或替代规则必须追加到本节之后。
- 验证方式：依赖检查、Schema 契约测试、架构检查和文档链接检查。
