{
  "name": "@aurora/ingestion-benchmark",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Aurora data ingestion end-to-end capacity and resilience benchmark tool (test/dev only)",
  "engines": {
    "node": ">=24.18.0 <25"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --exclude test/integration/**",
    "test:integration": "vitest run test/integration --no-file-parallelism",
    "test:coverage": "vitest run --coverage --no-file-parallelism"
  },
  "dependencies": {
    "@aurora/event-schema": "workspace:*",
    "@aurora/ingestion-api": "workspace:*",
    "@aurora/ingestion-credentials": "workspace:*",
    "@aurora/ingestion-inbox": "workspace:*",
    "@aurora/ingestion-worker": "workspace:*",
    "pg": "8.22.0"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "@types/pg": "8.20.0",
    "@vitest/coverage-v8": "4.1.10",
    "fastify": "5.10.0",
    "node-pg-migrate": "9.0.0",
    "tsx": "4.23.1",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  },
  "aurora": {
    "layer": "tooling"
  }
}
