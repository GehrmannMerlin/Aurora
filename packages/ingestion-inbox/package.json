{
  "name": "@aurora/ingestion-inbox",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Aurora data ingestion durable Inbox data model, migrations, and atomic persistence",
  "engines": {
    "node": ">=24.18.0 <25"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --exclude test/integration/**",
    "test:integration": "vitest run test/integration --no-file-parallelism",
    "migrate": "tsx src/run-migrations.ts"
  },
  "dependencies": {
    "@aurora/event-schema": "workspace:*",
    "pg": "8.22.0"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "@types/pg": "8.20.0",
    "node-pg-migrate": "9.0.0",
    "tsx": "4.23.1",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  },
  "aurora": {
    "layer": "data"
  }
}
