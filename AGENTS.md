# AGENTS.md

## 1. 作用域与规则效力

本文件适用于 Aurora 仓库根目录及所有子目录中的 Agent、代码、测试、文档、脚本和技术决策。

在不与更高优先级的系统、开发者或用户指令冲突的前提下，所有 Agent 必须执行本文件。不能为了方便、速度或减少工作量跳过规则。

本文件是项目级执行入口。[AURORA_RULES.md](AURORA_RULES.md) 是自包含的开发上下文和最高级规则入口；六份长期规范是各领域的详细权威来源。

## 2. 新对话强制读取顺序

新对话或上下文丢失后，必须按照以下顺序恢复项目上下文：

1. 完整阅读 [AURORA_RULES.md](AURORA_RULES.md)；
2. 完整阅读 [核心业务 PRD](Auroa-PRD-业务逻辑汇总-v2.1-核心业务定稿版.md)；
3. 完整阅读 [Aurora 架构规范](<Aurora 架构规范.md>)；
4. 完整阅读 [Aurora 代码规范](<Aurora 代码规范.md>)；
5. 完整阅读 [Aurora 测试规范](<Aurora 测试规范.md>)；
6. 完整阅读 [Aurora 文档规范](<Aurora 文档规范.md>)；
7. 完整阅读 [Aurora ADR 规范](<Aurora ADR 规范.md>)；
8. 阅读任务相关的 accepted ADR、模块 README、公共 API、协议、测试、发布和运维文档。

Agent 在首次修改代码或正式文档前，必须完整阅读以上六份长期规范，不能只阅读摘要或标题。

如果新对话只需要快速恢复开发上下文，先完整阅读 AURORA_RULES.md；真正开始修改前仍必须补读六份规范。

## 3. 六份长期规范保护

以下六份规范不得删除、移动、重命名或复制成替代版本：

- Auroa-PRD-业务逻辑汇总-v2.1-核心业务定稿版.md；
- Aurora 架构规范.md；
- Aurora 代码规范.md；
- Aurora 测试规范.md；
- Aurora 文档规范.md；
- Aurora ADR 规范.md。

PRD 文件名中的 Auroa 是历史兼容路径，产品正式名称始终为 Aurora。

修改这些文件时：

- 历史原则不得删除；
- 历史原则不得覆盖或静默改写；
- 后续规则只能追加；
- 新规则必须记录状态、日期、Owner、影响范围、原因、兼容和验证方式；
- 替代旧规则必须追加 supersedes，旧规则原文继续保留；
- proposed 规则不约束正式开发；
- 只有 approved 规则正式生效；
- 错别字、编号、链接和 Markdown 格式可以非语义校正，但必须在文件末尾追加维护记录；
- 不得只修改 AURORA_RULES.md 或本文件而跳过对应领域规范。

## 4. 当前项目状态

Aurora 当前处于核心业务规则冻结、工程实现准备阶段。

已经确定：

- 第一版产品范围和非目标；
- 五大系统边界；
- Core、Browser、插件和框架适配的 SDK 分层；
- 同步可靠接收、异步处理模型；
- event-schema 作为公共事件协议唯一来源；
- 单向依赖和自动架构约束；
- 严格 TypeScript、测试、文档和 ADR 基线。

尚未通过正式决策确定：

- 前后端框架；
- 数据库、消息队列、缓存和搜索技术；
- Monorepo 工具和包管理器；
- 构建、部署和发布平台；
- 具体浏览器版本矩阵；
- SDK 包体积和性能预算数值。

Agent 不得将未确定技术选型写成既定事实或直接实施。

## 5. 第一版范围门禁

Aurora 第一版只聚焦：

- 账号、组织和项目；
- SDK 接入与安全采集；
- 数据接收、校验、可靠缓冲和异步处理；
- 问题聚合、定位和处理；
- 发布、部署和 Source Map；
- 基础告警和站内通知；
- 权限、隐私、资源额度、保留和删除。

不得自行加入：

- 完整工单、审批或即时通讯；
- Session Replay 和完整用户行为分析；
- 责任小组、关注成员和复杂自动分配；
- 大规模后台批量任务；
- 高级查询语言、动态基线和异常检测；
- 外部通知、值班、升级和 SLA；
- 收费、账单、支付和企业治理；
- AI 根因分析。

任务超出第一版范围时，停止相关实现并报告范围冲突。只有正式 PRD 追加规则批准后才能继续。

## 6. 接到任务后的强制流程

### 6.1 确认任务类型

先判断任务是：

- 回答或评审；
- 诊断；
- Bug 修复；
- 功能开发；
- 重构；
- 公共 API 或协议变更；
- 文档或规则变更；
- 发布、部署或运维。

回答、评审和诊断请求不自动授权代码修改。不要把局部请求扩展成新的系统或产品能力。

### 6.2 检查工作区

修改前必须：

- 查看仓库和分支状态；
- 检查未提交修改；
- 区分当前任务修改和用户已有修改；
- 不覆盖、不还原、不删除无关改动；
- 确认目标文件和影响模块；
- 检查适用的更深层 AGENTS.md。

### 6.3 判断是否需要 ADR

以下变化必须先创建 ADR：

- 新增、删除、合并一级系统；
- 改变模块职责或依赖方向；
- 修改 SDK Core、Browser、插件或框架适配分层；
- 引入或替换数据库、消息队列、缓存、搜索、对象存储或部署架构；
- 修改同步可靠接收和异步处理模型；
- 修改公共事件协议兼容策略；
- 公共 API 不兼容变更；
- 修改默认采集、安全、隐私、脱敏或浏览器承诺；
- 影响全项目的核心框架和工程基础设施；
- 迁移、回滚或替换成本较高的长期选择。

需要 ADR 时，只能先创建 proposed ADR。proposed ADR 未经非作者和必要领域评审变成 accepted 之前，不得实施受该决策影响的正式代码。

accepted 只代表决策批准，不代表实施完成；实施状态必须单独验证和记录。

### 6.4 设计和计划

开始实现前必须明确：

- 目标和非目标；
- 所属系统和模块；
- 输入、输出和公开接口；
- 允许和禁止依赖；
- 默认、失败和边界行为；
- 生命周期和资源释放；
- 并发、幂等、重试和上限；
- 数据校验、隐私和权限；
- 测试范围和验收命令；
- 文档、迁移和回滚影响。

复杂任务必须先写可执行计划，列出具体文件、步骤、命令和预期结果。

## 7. 架构强制约束

系统分为 SDK、数据接入、数据处理与存储、管理平台和公共协议五个边界。

依赖方向必须保持：

- event-schema → Core 的被依赖关系；
- Core 不得依赖 Browser；
- Browser 可以依赖 Core；
- 插件和框架适配只能依赖公开的 Core、Browser 或 SDK 接口；
- 服务端接入和处理可以依赖 event-schema；
- 管理平台只能通过公开 API 使用服务端能力。

禁止：

- 循环依赖和反向依赖；
- 跨包引用 src、internal 或私有文件；
- Core 直接访问 window、document、fetch、XMLHttpRequest 或浏览器存储；
- 插件访问 Core 私有状态；
- 插件之间依赖内部实现；
- 插件自行建立上报队列或网络通道；
- 管理平台直接访问数据库或内部消息队列；
- 服务端依赖 SDK 内部实现；
- 用 shared 包掩盖职责不清或循环依赖。

外部输入必须视为不可信数据，并执行运行时校验。TypeScript 类型断言不能代替运行时 Schema。

## 8. SDK 宿主安全约束

SDK 的最高优先级是不能影响宿主页面。

必须：

- 隔离 SDK 内部和单个插件异常；
- 防止重复初始化和重复监听；
- 保存原生 API 原实现；
- 保持代理调用语义；
- 支持 stop 和 destroy 后完整恢复；
- 限制队列、批次、字符串、数组、对象深度、重试和退避；
- 排除 Aurora 自身上报请求；
- 支持多个 SDK 实例隔离运行；
- 页面退出前只尽力发送高价值错误。

禁止：

- 覆盖 window.onerror 或 onunhandledrejection；
- 修改原生对象原型；
- 永久替换 fetch、XMLHttpRequest 或 history；
- SDK 错误向宿主页面抛出；
- 一个插件失败导致其他插件停止；
- 无限重试、无限队列和无限定时器；
- 在同步采集路径遍历完整 DOM、解析 Source Map 或执行大规模聚合；
- 绕过统一规范化、字段限制、脱敏、过滤、采样、队列和上报流程。

## 9. 隐私和数据规则

默认不得采集：

- 请求体和响应体；
- Cookie 和 Authorization；
- 表单内容、密码和验证码；
- 完整 DOM、页面文本和点击记录；
- 剪贴板和控制台日志正文；
- 完整 IP、设备指纹和浏览器指纹。

SDK 配置可以减少或进一步脱敏数据，不能绕过服务端强制过滤。服务端控制台不能远程强制浏览器采集开发者未启用的数据。

请求默认只监控同源；跨域业务接口必须显式加入允许列表。查询参数值默认移除，动态路径只做高置信度归一化。

任何新增采集字段、用户身份方式或敏感数据权限变化，都必须同步更新 PRD、SDK、协议、安全和测试文档，并评估 ADR。

## 10. 代码实现要求

- 全项目使用严格 TypeScript；
- 外部输入先使用 unknown；
- 公共函数声明参数和返回类型；
- 禁止无说明 any、@ts-ignore 和宽泛断言；
- 函数、文件和模块保持单一职责；
- 错误不得静默吞掉；
- 日志不得包含敏感数据；
- 公共 API 最小、明确、稳定；
- 不兼容 API 需要 accepted ADR、迁移方案和版本策略；
- 代码注释解释原因、兼容性、性能或安全取舍；
- 临时方案写明原因、删除条件、Owner 和清理任务；
- 不进行与当前任务无关的重构。

跨包调用只使用公开导出入口。事件类型、枚举和运行时 Schema 统一来自 event-schema。

## 11. 测试要求

新功能和 Bug 修复优先使用测试驱动方式。

Bug 修复原则上必须：

1. 复现问题；
2. 编写能够在修复前失败的回归测试；
3. 实施最小修复；
4. 证明回归测试通过；
5. 运行受影响测试。

根据风险选择：

- 单元测试；
- 跨模块集成测试；
- event-schema 契约测试；
- 真实浏览器测试；
- 关键端到端测试；
- 性能、稳定性、内存或压力测试。

核心模块覆盖率基线为行 85%、分支 80%；其他业务模块为行 70%、分支 60%。

不得：

- 编写无有效断言的测试；
- 只验证函数调用而不验证行为；
- 使用任意等待掩盖异步问题；
- 依赖测试执行顺序；
- 通过反复重跑掩盖不稳定测试；
- 删除失败测试恢复 CI。

## 12. 验证和完成声明

声称任务完成、修复、通过或可发布前，必须运行与修改范围匹配的新鲜验证命令并读取完整输出。

最低检查包括：

- 直接测试和受影响测试；
- TypeScript 类型检查；
- 静态检查；
- 契约测试；
- 构建和包导出检查；
- 必要真实浏览器和端到端测试；
- 文档链接、元数据和示例检查；
- Git 差异和工作区状态。

验证失败时报告实际失败，不得使用“应该通过”“看起来没问题”等推测性结论。

完成定义：

- 用户要求全部实现；
- 未扩大第一版范围；
- 架构和隐私边界保持；
- 必要 ADR 已 accepted；
- 测试覆盖正常、异常、边界和生命周期；
- 所需验证实际通过；
- 受影响文档同步；
- 历史规则未删除或覆盖；
- 没有无关修改；
- 交付说明包含验证证据和剩余风险。

## 13. 文档同步要求

每次代码或设计变更都必须判断并同步：

- PRD；
- 架构和 ADR；
- 模块 README；
- SDK 和公共 API；
- event-schema 和协议；
- 测试和兼容矩阵；
- 隐私与安全；
- 发布、迁移和运维。

无文档影响时也必须说明原因。

文档示例必须可验证。正式文档使用相对链接。模块文档靠近代码，跨模块设计集中维护。

规则变更只能追加到对应长期规范，随后同步 AURORA_RULES.md 和本文件的当前执行要求。

## 14. Git 与修改安全

- 开始前查看 git status 和当前分支；
- 不擅自清理、回滚或覆盖用户修改；
- 不使用 git reset --hard 或其他破坏性命令；
- 只暂存当前任务范围内的文件；
- 提交前检查完整 diff；
- 推送前确认远端和分支；
- 禁止 force push，除非用户针对明确分支和原因单独授权；
- 外部发布、PR 和合并必须在用户授权范围内执行。

## 15. 停止条件

遇到以下情况，停止受影响的实现并报告：

- 规则互相冲突；
- 任务超出第一版 PRD；
- 重大决策只有 proposed ADR；
- 权限、隐私或删除行为不明确；
- 需要覆盖用户未提交修改；
- 验证重复失败且根因未知；
- 需要扩大外部系统或发布权限；
- 无法确认破坏性操作目标；
- 文档与代码无法确定哪个是当前有效来源。

停止只影响存在风险的范围；仍可继续安全、只读和不依赖该决策的检查。

## 16. 当前 ADR

[ADR 索引](docs/adr/README.md)中的 ADR-001 至 ADR-006 当前均为 proposed、not-started。

在状态变为 accepted 前，不得把这些提案当成新增实施授权。现有已批准架构规范仍是当前架构基线。

## 17. 规则维护记录

### AGENT-RULE-BASELINE-20260727：项目级 Agent 约束基线

- 状态：approved
- 生效日期：2026-07-27
- Owner：architecture
- 影响范围：Aurora 仓库及全部子目录
- supersedes：none
- 维护方式：append-only
- 基线说明：本文件首次建立 Agent 强制读取、范围门禁、ADR 门禁、架构、SDK、安全、代码、测试、验证、文档和 Git 行为约束。
- 历史保护：后续规则只能追加，不得删除、覆盖或静默改写本基线。
- 同步要求：Agent 规则变化必须先追加到对应长期规范，再同步 AURORA_RULES.md 和本文件。
