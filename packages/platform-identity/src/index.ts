/**
 * @aurora/platform-identity — Aurora platform identity, authentication and
 * invitation data layer.
 *
 * This module is the package root. Task 2 scaffolds the package (build/typecheck/
 * migrate entry + the 11-table data model migration). Task 3 fills in the
 * repository, Argon2id password and intent-token API. The surface below is the
 * minimal stable placeholder exported so the built package entry is verifiable.
 */
export const PLATFORM_IDENTITY_PACKAGE = '@aurora/platform-identity' as const;

export const PLATFORM_IDENTITY_VERSION = '0.0.0' as const;
