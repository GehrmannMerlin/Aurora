---
title: ADR-002：划分五大系统边界
status: accepted
implementation-status: not-started
owner: architecture
date: 2026-07-27
last-reviewed: 2026-07-29
applies-to: Aurora 全部一级系统
related:
  - ../../AURORA_RULES.md
  - ../../Auroa-PRD-业务逻辑汇总-v2.1-核心业务定稿版.md
  - "../../Aurora 架构规范.md"
  - ../README.md
  - ../architecture/system-overview.md
  - ../architecture/sdk-architecture.md
  - ../architecture/platform-frontend.md
  - ../architecture/platform-backend.md
  - ../architecture/deployment.md
  - ADR-004-asynchronous-event-processing.md
  - ADR-005-event-schema-source-of-truth.md
  - ADR-006-one-way-dependencies.md
supersedes: none
superseded-by: none
---

# ADR-002：划分五大系统边界

## 元数据

- 状态：accepted
- 日期：2026-07-27
- Owner：architecture
- 适用范围：Aurora SDK、数据接入、数据处理与存储、管理平台、公共协议
- 关联 PRD：[核心业务 PRD](../../Auroa-PRD-业务逻辑汇总-v2.1-核心业务定稿版.md)
- 关联技术方案：[Aurora 架构规范](<../../Aurora 架构规范.md>)
- 关联 Issue：none
- 关联实现 PR：none
- 替代 ADR：none
- 被替代 ADR：none
- 实施状态：not-started
- 评审状态：非作者及所需领域评审已通过

## 背景

Aurora 从浏览器捕获事件，到服务端接收、异步处理、查询和管理平台展示，存在不同运行环境、可靠性要求和安全边界。如果按页面、部署单元或开发人员随意组织，容易出现 SDK 承担服务端逻辑、管理平台直连数据库或协议定义分散。

## 决策驱动因素

- SDK 不能影响宿主页面；
- 接入路径需要快速、可靠和可限流；
- Source Map、聚合和告警属于耗时异步处理；
- 管理平台必须通过稳定 API 使用数据；
- 公共事件协议必须跨端一致；
- 第一版允许同一应用部署多个逻辑模块，但代码边界不能合并。

## 候选方案

### 方案 A：五大职责系统

划分 SDK、数据接入、数据处理与存储、管理平台、公共协议五个一级系统。

优点：

- 每个系统有清晰输入、输出和非职责；
- 运行环境和可靠性要求自然分离；
- 公共协议拥有独立权威位置；
- 允许早期合并部署而保持代码边界；
- 便于针对系统风险设计测试和监控。

缺点：

- 需要维护接口和依赖规则；
- 数据接入与处理之间需要可靠缓冲；
- 小规模阶段看起来比单体目录更复杂；
- 跨系统功能需要明确归属。

### 方案 B：按可部署应用划分

只划分浏览器 SDK、服务端应用和管理平台，接入、处理、协议都放入服务端。

优点：

- 初始目录和部署简单；
- 服务端内部调用直接；
- 少量开发者容易快速开始。

缺点：

- 接入与耗时处理容易耦合；
- 协议可能成为服务端私有定义；
- 管理平台可能依赖数据库模型；
- 服务端单体随着聚合、告警、Source Map 增长而职责失控。

### 方案 C：从第一天拆成微服务

接入、队列消费者、Source Map、聚合、查询、告警和权限分别独立部署。

优点：

- 运行和扩容边界清晰；
- 故障和资源可以隔离；
- 适合高事件量和大团队。

缺点：

- 第一版运维、网络和数据一致性复杂度过高；
- 需要服务发现、追踪、部署和故障治理；
- 很多容量假设尚无真实数据；
- 影响核心产品闭环交付速度。

## 最终决策

决定选择方案 A：五大职责系统。

五大系统是逻辑和代码边界，不要求第一版立即拆成五个独立部署单元，也不选择具体队列、数据库、缓存、搜索或物理服务拓扑。

## 结果与影响

### 正面影响

- SDK、接入、处理和平台职责清楚；
- 耗时处理不会阻塞上报请求；
- 协议和运行时校验可复用；
- 管理平台不会扩散存储内部结构；
- 后续可以按真实规模拆分部署。

### 负面影响与代价

- 需要定义跨系统接口和失败语义；
- 早期同部署时仍要维护逻辑隔离；
- 事件状态和可观测性需要跨阶段设计；
- 开发者必须理解系统归属而不能随意放置代码。

### 未解决问题

- 各系统的具体部署拓扑；
- 队列和数据库技术；
- 查询服务是否独立部署；
- 组织和权限模块的物理位置。

## 实施约束

- SDK 只采集、处理并上报标准事件；
- 接入只做鉴权、限制、校验、过滤和可靠接收；
- 数据处理负责清洗、分组、Source Map、聚合、存储和告警；
- 管理平台只通过公开 API；
- 公共协议不依赖业务系统；
- 同部署不允许跨越公开接口直接访问其他系统内部实现；
- 每个模块 README 声明职责、非职责、输入、输出和依赖。
- 跨系统删除、保留与秘密传播必须有明确 Owner、可追踪状态和逐系统完成确认，不能把局部成功描述为全局完成。

## 迁移方案

ADR accepted 后，在 Monorepo 中为五大系统建立明确模块或目录。第一版可以让接入、处理和查询在同一服务端应用部署，但使用模块接口、队列或任务边界隔离。随着事件量和团队规模增长，再通过新 ADR 评估物理拆分。

## 回滚方案

如果评审认为五大边界不适用，可在正式代码形成前撤回本提案。实施后改变一级系统必须创建新 ADR、列出数据和接口迁移，并保留旧边界文档。

## 验证方式

- 每个模块 README 有职责和非职责；
- 管理平台无数据库和内部队列依赖；
- SDK 无服务端分析和 Source Map 逻辑；
- 接入请求不执行复杂聚合；
- event-schema 无业务模块依赖；
- 架构检查阻止跨边界私有引用。

## 重新评估条件

- 事件量要求独立扩缩容；
- 某系统的职责持续变化或无法独立测试；
- 部署或组织结构发生重大变化；
- 五大边界造成明显重复或不必要网络调用；
- 新业务超出前端监控范围。

## 追加记录

本 ADR 的评审、状态、实施和替代变化只能追加在本节之后。

### 2026-07-29：正式化复审输入

- 状态保持 `proposed / not-started`；本次没有产生评审批准或实施证据；
- 背景输入补充：[系统架构与模块边界](../architecture/system-overview.md)已经把 SDK、数据接入、数据处理与存储、管理平台和公共协议作为稳定职责边界正式承载；[SDK 架构](../architecture/sdk-architecture.md)、[平台前端架构](../architecture/platform-frontend.md)和[平台后端架构](../architecture/platform-backend.md)进一步证明各边界的运行环境、输入输出与失败语义不同；
- 候选方案复审：方案 A 继续作为提案；方案 B 会把接入、异步处理和协议重新耦合，方案 C 则在没有容量与团队证据时预设微服务。已批准的 AWS 拓扑只提供部署输入，不等于把五个逻辑系统一一部署成五个服务；
- 实施约束补充：管理平台只消费公开 API，公共协议不依赖业务系统，接入成功与处理完成必须分离，逻辑边界不得因模块化单体或共享运行时而失效；
- 验证输入补充：正式审批应以端到端事件链路、权限查询链路、Source Map/告警异步链路和部署拓扑逐项检查所有权、依赖方向与故障隔离；物理拆分阈值标记为 `requires-benchmark`；
- 进入 `accepted` 前仍需非作者、SDK、服务端、平台和基础设施领域评审，并确认本 ADR 不选择具体队列、数据库、缓存或搜索技术。

### 2026-07-29：接受决策

- 决策状态更新为 `accepted`，实施状态保持 `not-started`；
- 独立非作者评审由隔离审查上下文 `adr_001_003_review` 完成，覆盖 architecture、SDK、ingestion、processing、platform、security 和 infrastructure 视角；
- 评审确认五大职责、按部署应用和首日微服务三项候选真实，逻辑边界与部署拓扑、可靠接收、协议来源和依赖约束的职责拆分清楚；
- 安全与隐私复核确认管理平台只经公开 API，跨系统删除、保留和秘密传播需要显式 Owner 与逐系统确认；
- 当前没有真实模块、公开接口、部署单元、Issue、实现 PR 或测试结果，本次接受不得解释为五个系统已实现或已物理拆分。
