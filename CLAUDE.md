# Aurora Claude Code 项目入口

@AGENTS.md
@AURORA_RULES.md

不要复制 AGENTS.md、AURORA_RULES.md 或六份长期规范的全文。本文件只保留角色模式、任务开始规则、自主决定边界、Git 安全、停止条件和完成报告的当前快照；详细规则以对应权威文档为准。

## 角色模式

Claude Code 可以根据本轮用户指令工作于以下模式：

### 规划者模式

当用户要求设计、正式化、制定规格或编写计划，且没有要求立即实施时：

- 使用 Superpowers brainstorming；
- 根据 approved 文档和 accepted ADR 形成正式规格；
- 使用 writing-plans 编写单模块实施计划；
- 不执行计划；
- 不编写实施代码；
- 计划完成后等待用户或其他 Agent 接手。

### 执行者模式

当用户提供了已经批准的规格和实施计划，并要求执行时：

- 不重新设计；
- 不重写计划；
- 使用 subagent-driven-development 或 executing-plans；
- 严格按 Task 和 TDD 顺序执行；
- 完成真实质量验证。

Codex、Claude Code 或其他 Agent 编写的计划一视同仁；权威性来自规格、ADR、计划状态和用户授权，而不是创建计划的工具名称。

### 联合模式

当用户明确要求 Claude Code 同时规划和实施时：

- 使用 brainstorming 形成单模块规格；
- 对规格执行完整自检；
- 使用 writing-plans 生成单模块计划；
- 对计划执行完整自检；
- 若没有新增产品、架构、安全或隐私决策，则根据用户的联合模式授权立即实施；
- 使用 subagent-driven-development 或 executing-plans；
- 完成后使用 verification-before-completion。

联合模式不得一次规划或实施整个项目。

### 模式判定优先级

按以下顺序决定角色：

1. 本轮用户明确指令；
2. 是否已经存在 approved 规格和可执行计划；
3. `AGENTS.md` 和 `AURORA_RULES.md` 的当前阶段；
4. 默认只执行当前最小模块，不自行扩展到后续模块。

已有计划时，不得因 Claude Code 具备规划能力而重写计划。没有计划但用户只要求执行时，必须报告缺少计划，不得临时边写边做。用户明确要求联合模式时，可以在同一会话完成规格、计划和实施。

### 权威优先级

发生冲突时：

1. 用户当前明确指令；
2. approved 核心 PRD 和长期规范；
3. accepted ADR；
4. approved 正式模块规格；
5. approved 专题设计；
6. 当前模块实施计划；
7. Agent 的普通实现判断。

实施计划不能覆盖上级权威来源。

### 高效上下文加载

每个模块默认只读取：

- `AGENTS.md` 和 `AURORA_RULES.md`；
- 当前模块直接规格；
- 直接依赖包的公共出口、README 和相关测试；
- 与当前模块相关的 ADR；
- 长期规范中通过搜索命中的相关章节。

不得每轮默认全文读取：整份 PRD、全部长期规范、所有 ADR、所有专题设计、所有旧实施计划、与当前模块无关的源码和测试。只有发现冲突、接口不一致或语义缺失时才扩大读取范围。

## 任务开始规则

开始任何任务前必须：

1. 检查当前分支、`git status`、已暂存内容、未提交修改和未跟踪文件；
2. 保护用户已有修改，不得覆盖、删除、重置或顺手整理无关文件；
3. 根据 `AGENTS.md` 和 `AURORA_RULES.md` 的任务路由读取相关权威文档；
4. 只使用 `approved` 正式文档和 `accepted` ADR 作为实施依据；
5. 规划者模式不要求预先存在实施计划；执行者模式必须完整读取指定计划；联合模式先生成计划，再完整读取并执行；
6. 所有模式都必须保护工作区和已有修改。

## 实施计划执行规则

收到实施计划后必须：

- 严格按照计划的 Task 顺序执行；
- 一次只处理一个 Task；
- 不跳过失败测试、失败确认、最小实现和通过验证；
- 不把后续 Task 的内容提前塞入当前 Task；
- 不实现计划排除的模块；
- 不自行修改公共 API、架构边界或长期技术选择；
- 使用计划中给出的精确文件路径、接口、命令和预期结果；
- 实际结果与计划不一致时先调查原因，不得伪造通过结果；
- Bug 修复和新增行为必须有对应自动化测试；
- 每个 Task 完成后运行该 Task 的目标测试；
- 全部 Task 完成后运行计划要求的完整质量门禁；
- 同步更新计划要求的 README 和正式文档。

## 自主决定边界

允许自行决定：

- 不改变公共行为的局部实现细节；
- 符合现有规范的私有函数和文件拆分；
- 测试数据、私有命名和最小内部实现；
- 已批准技术栈内的普通配置。

不得自行决定：

- 改变 approved 产品规则；
- 修改 accepted ADR 的最终决策；
- 扩大公共 API；
- 改变模块职责或依赖方向；
- 新增数据库、队列、缓存、云服务或长期基础设施；
- 修改安全、隐私、数据保留和兼容承诺；
- 跳过测试或降低质量门禁。

## Git 与工作区安全

除非本次用户指令明确授权：

- 不执行 `git add`；
- 不执行 `git commit`；
- 不执行 `git push`；
- 不执行 `git reset`、`git rebase` 或丢弃修改；
- 不把任务外文件包含进提交；
- 不修改用户已有的无关工作区差异。

计划中的 Commit 步骤默认只作为提交边界记录；没有明确授权时，不实际创建提交。

## 停止条件

只有出现以下情况时才停止并报告：

- 实施计划与 approved 规范或 accepted ADR 直接冲突；
- 必需的上游文件、接口或依赖不存在；
- 继续执行可能覆盖用户修改；
- 需要不可逆数据操作或破坏性 Git 操作；
- 测试证明计划中的接口或假设无法成立；
- 必须改变公共行为、架构、安全或隐私规则才能继续。

普通实现选择不得重新发起产品设计讨论。应采用最小、保守、可测试且符合规范的实现。

## 完成报告

任务结束时报告：

1. 完成的 Task；
2. 创建和修改的文件；
3. 测试与质量命令的真实结果；
4. 与计划的偏差及原因；
5. 未完成事项；
6. Git 工作区状态；
7. 是否修改了公共接口、ADR、正式规范或计划范围；
8. 明确说明是否执行了提交或推送。
